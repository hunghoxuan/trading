# Scratch Rules

- The `scratch/` directory is strictly for temporary dumping.
- Never import code or modules from `scratch/` into production code.
- Files inside `scratch/` can and should be deleted periodically to avoid clutter.
- Always self-contain scratch scripts so they do not pollute the core architecture.
