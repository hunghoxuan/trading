{
  "name": "telegram-signal-bot",
  "version": "1.1.0",
  "private": true,
  "description": "Lightweight webhook receiver that forwards trading signals to Telegram",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test:smoke": "node ../scripts/test_local_stack.mjs",
    "test:remote": "bash ../scripts/test_remote_api.sh"
  },
  "dependencies": {
    "pg": "^8.20.0"
  },
  "engines": {
    "node": ">=18"
  }
}
