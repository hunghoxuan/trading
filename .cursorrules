# Global Agent Constraints

You are interacting with the Trading Bot Workspace.

IMPORTANT RULE:
Before you make any plans, execute any code, or answer any architectural questions, YOU MUST manually read the local bootloader file at:

`.agents/README.md`

This file will dynamically route you to the Active Sprint board (`sprint.md`), the strict behavioral rules (`rules.md`), and the inter-agent `MAILBOX.md`.
Do not hallucinate past state. Rely entirely on the `.agents/` tracking layer.
