0331-8

- Fixed Pine syntax error in `Hung - MSS` around SR visual line updates.
- Corrected indentation in `draw_data_sr_visual(...)` so nested `if` blocks parse correctly.
- Updated `@file-version` in `Hung - MSS` to `0331-8`.
