Snapshot `0331-3`

- Set all three strategy scripts to `currency = currency.USD`.
- Removed strategy order comments from `strategy.entry()` and `strategy.exit()` so chart order markers no longer show model text like `BO.E`, `TP`, `SL`.
- Disabled chart-side custom strategy visuals in `Hung - MSS`, `Hung - SMC`, and `Hung - Core`.
