# Project Roadmap

- Project: ICT-SMC-PA All-in-one
- Source: `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` + `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` + `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`
- Last Updated: 2026-03-18

## Done Features (Name + Timestamp)
- 2026-03-18: ICT active-area selector simplified: `activeTradingHigh/Low` now derive from nearest HTF1 swing above/below current close (removed mixed selector score path and legacy candidate helpers).
- 2026-03-18: ICT constant-trim pass (no logic expansion): removed old dashboard-position enums/settings in ICT, removed legacy entry-visual constants, inlined zone-quality defaults into config init, and switched HTF visual refs to TF configs (`ltfCfg/htf1Cfg/htf2Cfg`) where applicable.
- 2026-03-18: Runtime-offset safety pass: tightened live-scan/history caps in ICT + Core (`get_range_extreme_from_index` call paths, pivot/lookback offsets) to reduce historical-buffer overflow risk on deep history bars.
- 2026-03-16: ICT entry-visual mode routing fix: resolved entries now force `ENTRY_VIS_RESULT_MODE` per-entry at render call, while `START` entries keep `ENTRY_VIS_MODE`, preventing TP/SL entries from remaining in normal-plan visuals.
- 2026-03-16: Restored HTF candle panel visibility under strict modes by always running `process_data_htf_candles(...)` against cached snapshots (heavy refresh still strict-gated); updated ICT Buy/Sell defaults to production baseline (`HTF/OB/BB/FVG/iFVG/RJB/Sweep = ON`, `Resolve Entry = Bar Close`, `Show TP/SL = ON`, `Max START = 1`).
- 2026-03-16: Dashboard runtime safety fix in Core: guarded `render_strategy_stats_table(...)` loop with `rowCount > 0` to prevent empty-array `array.get()` crashes when no resolved entry types exist.
- 2026-03-16: Strict-mode performance pass (ICT): added snapshot caching for heavy `get_data_*` paths (`HTF zone snapshots`, `HTF candles`, `market-structure data`) and refreshes them only on `strictRun` (or first bar), reducing unnecessary per-tick recomputation in `Bar Close` / `New Bar` modes.
- 2026-03-16: Strict-mode dashboard visibility fix: entries dashboard now renders in all resolve modes (not only Intrabar); ICT now caches and re-renders events dashboard text outside strict gate to keep UI visible between strict-run ticks.
- 2026-03-16: Full strict-mode execution gate added: both ICT and Indicators now derive `strictRun` from Entry Resolve Mode and gate major `process_*`/`draw_*` runtime blocks accordingly (not only entry resolve), enforcing consistent per-mode execution cadence.
- 2026-03-16: Entry resolve-mode strictness pass: in both ICT and Indicators, entry pipeline now runs as one unit by mode (resolve + visual refresh + stats), i.e. no mixed behavior where visuals update every tick while resolve is close/newbar-only.
- 2026-03-16: Added configurable entry lifecycle resolve mode in both ICT and Indicators (`Intrabar`, `Bar Close`, `New Bar (Prev Candle)`), so TP/SL+touched updates can run intrabar or only on confirmed/previous bar while keeping existing entry-creation logic unchanged.
- 2026-03-16: Entry visual state refinement in Core `draw_entry_visual(...)`: resolved TP/SL now render winner-side background only, loser-side border-only, dotted TP/SL borders + dotted entry line, and resolved entry line color follows outcome side (green for TP, red for SL).
- 2026-03-16: Visual-config override normalization pass: standardized a single `Visual Config Overrides` block in both ICT and Indicators scripts, centralized indicator-level UI config objects (`ENTRY_VIS_CFG`, `ZONE_VIS_CFG`, `HTF_BOX_VIS_CFG`, `SR/EQ/LIQ/EVT` line cfgs, and candle-pattern visual cfg), and removed remaining inline visual-config creation from runtime logic paths.
- 2026-03-15: Moved ICT helper groups to Core (batch 1/2/3): TF-source color/ext helpers, lifecycle merge/name/tooltip helpers, and generic events-dashboard helpers (`get_events_dashboard_text`, `draw_events_dashboard_cell`); ICT callsites now use `CORE.*` for these paths.
- 2026-03-15: Moved dynamic entries-dashboard aggregation/rendering into Core (`render_entries_dashboard_from_entries(...)`) and replaced ICT local dashboard loops with direct Core calls for all dashboard positions.
- 2026-03-15: BOS/MSS text-repeat fix in ICT: added structure emit dedupe gate keyed by `(tf, break-side, pivotTime)` so the same broken level label is drawn once instead of re-emitting repeatedly on subsequent bars.
- 2026-03-15: ICT compile hotfix at structure pipeline: replaced invalid tuple ternary assignment in `process_tf_structure_state(...)` with explicit local init + guarded tuple assignment (`if enabled`) to resolve syntax error at line 1397.
- 2026-03-15: Scope-budget rescue pass in ICT: removed/inlined multiple single-use wrapper functions (HTF cleanup wrapper, PD/PW draw wrappers, active-area wrapper, entries-window wrapper, liquidity/SR/EQ get/draw wrappers, structure passthrough wrapper) to reduce Pine local-scope count without changing trading logic.
- 2026-03-15: Scope reduction for dynamic entries dashboard: removed heavy local renderer in ICT and switched to compact runtime aggregation (`dashNames` + `dashStats`) rendered via `CORE.render_strategy_stats_table(...)`.
- 2026-03-15: ICT entries dashboard changed from fixed setup rows to dynamic rows built from `entries[]` (resolved entries only). Setup names are discovered at runtime, aggregated per type (TP/SL/WR/+R), and rendered with dynamic row count (up to configured max types).
- 2026-03-15: Added ICT Buy/Sell setting `Entry Value` with options `Zone Edge` and `Zone Middle`; `add_entry(...)` now sets entry to zone midpoint when `Zone Middle` is selected (existing SL/TP pipeline unchanged).
- 2026-03-15: Dynamic-TP stats refinement in ICT: when `Dynamic TP` is enabled, resolved entry gain now uses `ceil(realizedR)` where `realizedR = abs(exitPrice-entry)/abs(entry-stoploss)` and `exitPrice` is TP/SL fill; fixed-TP mode keeps planned `rrValue`.
- 2026-03-15: ICT Buy/Sell settings now expose `Dynamic TP` toggle (`RR` row). Entry planner uses this input directly (`signalDynamicTp`) instead of fixed constant.
- 2026-03-15: ICT Buy/Sell settings now support START-entry cap (`Limit START Entries`, `Max START`); new entries are blocked when current START count reaches the configured max while existing START entries can still be updated/resolved.
- 2026-03-15: Scope-limit hotfix: removed HTF panel vertical-normalization helpers from ICT file to recover Pine local-scope budget; retained only HTF1↔HTF2 dynamic candle-count recalculation logic.
- 2026-03-15: HTF candle slot stability fix: removed hard reset on dynamic candle-count drift (reset now only on TF change/first bar) so HTF2 panel no longer collapses to a single candle when dynamic counts fluctuate.
- 2026-03-15: HTF candle-count balancing pass: dynamic counts now use two-pass ratio normalization (`HTF2 = HTF1/ratio`, then `HTF1 = max(HTF1, ceil(HTF2*ratio))`) to keep HTF1/HTF2 panels on equivalent time span and reduce slot drift.
- 2026-03-15: Fixed HTF mini-candle overlap bug in Core `render_htf_view(...)`: render loop now clamps to requested `count` and hides stale extra objects when store size exceeds count, preventing last-candle stacking artifacts.
- 2026-03-15: Core entry draw boundary rule updated in `draw_entry_visual`: if `barClosed` exists, right edge is exactly `barClosed`; otherwise right edge is `bar_index + extendBars`.
- 2026-03-15: Unified TF line-extension policy in ICT: `LTF/HTF1/HTF2 line ext = zone ext + 3` via `*_LINE_BARS_EXT`; applied to Fib (all TFs) and SR line placement so each TF’s line labels align on the same bar and lines extend farther than OB/FVG zones.
- 2026-03-15: Fixed HTF2-zone disappearance after TF suffix removal: HTF zone dedupe no longer keys on label text (`OB`/`FVG`) and now dedupes by same type+side+slot (`extendBars`) + overlap, allowing HTF1 and HTF2 to coexist.
- 2026-03-15: Compatibility rollback for live compile: ICT import pinned back to `TradingKitCore/16` so `activeZones: array<CORE.PriceZone>` matches currently published `TradingKitSMC/21` signatures.
- 2026-03-15: Library type-alignment fix prep: `TradingKitSMC` now imports `TradingKitCore/18` to match ICT `CORE.PriceZone` type and avoid cross-version `array<PriceZone>` mismatch.
- 2026-03-15: Core entry renderer rule pass (`draw_entry_visual`): TP path forced green family, SL path forced red family; touched entries use solid/clearer visuals, untouched entries use dotted/more transparent visuals; entry marker text changed from arrow to `setupTxt`; tooltip compacted to 3-line format (`SIDE (SETUP)`, `entry/tp/sl/RR`, `reason`).
- 2026-03-15: Fixed ICT compile type-mismatch (`array<PriceZone>` across imports) by aligning `Hung - ICT SMC Zones` to `TradingKitCore/16` to match `TradingKitSMC/21`.
- 2026-03-15: Restored entry TP/SL box backgrounds in shared Core renderer (`draw_signal_plan` + `draw_entry_visual` update path) so active entries are no longer border-only.
- 2026-03-15: Entry visual ownership simplified in ICT: removed ICT-side TP/SL recolor override and now uses shared `CORE.draw_entry_visual(...)` path only; Events dashboard feed now snapshots queue after `_controlZone(...)` and before event consumption so non-empty pending events are visible.
- 2026-03-15: HTF projected zone labels normalized to type-only text (`OB`/`FVG` no TF suffix), and Events dashboard moved to top-left for visibility (no overlap with entries dashboard slots).
- 2026-03-15: Unified runtime containers in ICT Zones: replaced split event arrays with `eventsQueue`, replaced split horizontal level arrays with one `levels` store (`PriceZone` for SR/LIQ/EQH/EQL), and renamed HTF candle stores to `htf1_candles` / `htf2_candles` for explicit semantics.
- 2026-03-14: TF visual normalization follow-up: all fib labels now ratio-only (`0.5/0.6/0.7`), zone/BOS-MSS/SR-sweep text colors align to TF color mapping, mitigated zone labels use blurred TF color, and HTF zone borders switched to very-blur TF-slot colors (1px).
- 2026-03-13: TF visual consistency pass: unified per-TF structure line color usage for BOS/MSS/fib/sweep-related liquidity lines, switched HTF zone borders to TF-slot colors, standardized labels (`base TF` lower-case without TF suffix, `HTF` upper-case with TF suffix), shortened base-TF fib span vs HTF spans, and simplified Indicators HTF dashboard cells to TF text only with TF-colored text.
- 2026-03-13: True split phase-2 (batch-2) completed: moved remaining ICT/SMC domain helpers from Core-callers to `TradingKitSMC` (`try_merge_ob_sd_zone`, HTF zone/SR snapshot builders, SR label/source/x helpers, and overlap wiring), rewired ICT callsites to `SMC.*`, and kept Core as generic utility surface.
- 2026-03-13: Split integrity hardening: fixed malformed local function signatures introduced during prior export-trim pass in `TradingKitCore`, synced imports to `TradingKitCore/5` and `TradingKitSMC/13`, and revalidated that Core/SMC have zero shared exported method/type names.
- 2026-03-13: True split phase-2 (batch-1): moved SMC-specific exported methods from `TradingKitCore` to `TradingKitSMC` (`zone_formation_allowed`, `smc_is_ob/fvg`, OB label/border/taxonomy helpers, signal-type/bias gates, active-area level-touch helpers, etc.), rewired ICT callsites to `SMC.*`, and left Core with private internal fallbacks only where needed for Core-internal functions. Result: zero shared exported method/type names between Core and SMC.
- 2026-03-13: Library split bootstrap completed: `TradingKitSMC` rewritten as SMC-domain-only type library (no duplicated methods/types from `TradingKitCore`), and ICT migrated to consume SMC domain types via `import hunghoxuan/TradingKitSMC/12 as SMC`.
- 2026-03-13: ICT local runtime safety fallback added: replaced live-tail calls to `LIB.get_range_extreme_from_index(...)` with local `get_range_extreme_from_index_safe(...)` that skips offsets above safe cap, preventing sporadic historical-buffer overflow on long legs.
- 2026-03-13: Stability/trim sweep: tightened setter guards in horizontal-level hot loops (`LIQ`/`EQ`/`SR`) to reduce redundant `set_*` calls and synced feature docs to current `seed -> shared lifecycle engine -> render` architecture.
- 2026-03-13: Runtime safety hotfix for live-tail scan: capped `get_range_extreme_from_index(...)` scan bounds in TradingKitCore/TradingKitSMC and switched ICT live-tail callers to `LIVE_SWING_SCAN_MAX_BARS=300` to prevent historical-buffer offset overflow.
- 2026-03-13: Unified horizontal lifecycle engine added for `PriceZone` key-level families (`LIQ`, `EQ`, `SR`) via `update_horizontal_levels(...)`; per-type behavior now branches inside one shared update loop while seed pipelines remain separate.
- 2026-03-13: Shrink pass (no behavior change): removed temporary EQ source-mode scaffolding, removed unused `LIQ_PIVOT_LEN`, removed low-value EQ wrapper (`seed_from_eq_pairs`), and simplified unified swing-seed dispatch from string kind to boolean flag.
- 2026-03-13: Unified seed interfaces in ICT (`seed_from_swings`, `seed_from_eq_pairs`), migrated liquidity seeding from standalone pivots to TFData swing ledgers (HTF2/HTF1/+optional base TF), and aligned post-seed lifecycle pruning path across SR/EQ/Liquidity via shared level-drop checks.
- 2026-03-13: SR stabilization pass: near-level merge and max-level pruning now use unified SR strength ranking (HTF source priority first, then touch strength, then proximity) to reduce noisy/random SR replacements.
- 2026-03-13: SR seeding now uses HTF2+HTF1 swings by default with runtime toggle `SR Use Base TF Seeds` for optional current-TF swing seeding; unified zigzag visuals to dotted 1px style with per-TF transparency and same-color swing markers (LTF/HTF1/HTF2).
- 2026-03-11: Refactor batch (ordered 5→4→1→2→3): moved remaining ICT helpers to `TradingKit/56` (`get_mimic_pivot_len`, `draw_htf_swing_if`, `update_selector_bound`), added zone-id constants for int-based branching in `_controlZone`, reduced key-level lifecycle duplication via shared remove/drop helpers, staged `_controlZone` visuals via `update_zone_box_label` + `mark_zone_mitigated_visual`, and cached candle inputs (`o0/h0/l0/c0`, `o1/h1/l1/c1`) per `_controlZone` call.
- 2026-03-11: ICT compacting pass: merged three event confirmation processors into unified `process_event_queue(...)`, replaced dual active-area scans with single-pass `get_active_trading_bounds()`, extracted shared SR visual updater `refresh_sr_visual(...)`, and unified strategy metadata lookups via `get_strategy_meta(...)`.
- 2026-03-11: Moved reusable ICT helpers to `TradingKit/55`: `apply_line_pricezone_visual`, pivot/leg utilities, range-extreme utility, and HTF zone/SR snapshot tuple builders; rewired ICT to `LIB.*` and removed local duplicates.
- 2026-03-11: ICT structure/layout cleanup: moved ICT `INPUTS` block below global persistent state so business state is grouped near the top for faster process tracing.
- 2026-03-11: UI-helper migration pass: removed local entry-stats renderers in ICT/Indicators and switched both to shared `TradingKit` renderers (`render_signal_stats_table`, `render_strategy_stats_table`).
- 2026-03-11: Optimization batch (safe): reduced HTF security calls by batching HTF2 zone+SR snapshot into one `request.security(...)`, added early old-zone prune in `_controlZone(...)`, and centralized repeated tooltip prefixes via shared `TradingKit.Settings` fields (`TOOLTIP_REASON_PREFIX`, `TOOLTIP_TOUCHES_PREFIX`) used by ICT + Indicators.
- 2026-03-11: Dead-reference cleanup pass: removed unused ICT helper `get_last_high_low_pair_leg(...)` and removed unused Indicators constants `SYM_BUY` / `SYM_SELL` (no logic/visual behavior change).
- 2026-03-11: Final cleanup pass (non-semantic): removed dead `KeyLevel` type from TradingKit (no remaining references), normalized strategy helper names in Indicators (`is_strategy_applicable`, `get_strategy_disp`, `is_strategy_enabled`), normalized ICT strategy helper names (`is_strategy_enabled_by_id`, `get_strategy_short_name`), and trimmed `ZoneDrawConfig` to used fields only.
- 2026-03-11: One-pass non-semantic unification cleanup: normalized ICT local UI helper names to `draw_*` / `delete_*`, removed dead legacy alias `useStratCpBias` in Indicators, and consolidated active-trading high/low line-label rendering through shared local helpers.
- 2026-03-11: Safe naming unification pass in ICT: replaced stale `apply_sr_zone_visual(...)` call sites with unified `apply_line_pricezone_visual(...)` helper (no behavior/logic change).
- 2026-03-11: Event model unification step-1 completed: replaced `EntrySignal`, `SweepReclaimEvent`, and `BreakRetestEvent` usage with shared `Event` type across TradingKit + ICT + Indicators, including queue handlers and registration helpers.
- 2026-03-11: Type unification step-2 (phase A) completed: migrated ICT horizontal level families (`liquidityLevels`, `eqLevels`, `srLevels`) from `KeyLevel` arrays to `PriceZone` arrays; updated TradingKit SR/liquidity helpers to operate on `PriceZone`.
- 2026-03-10: Unified entry add/resolve policies via `TradingKit/49`: added shared helpers (`create_entry_if_valid`, `apply_entry_outcome_stats`), entry policy settings (`ENTRY_REQUIRE_NEXT_BAR`, `ENTRY_SL_FIRST_ON_DUAL_HIT`, `ENTRY_RECENT_DUP_BARS`), and rewired both ICT + Indicators to use `resolve_entry_state(...)` with shared settings.
- 2026-03-10: Migrated strategy marker/entry rendering helpers and `Strategy(shortName)` type into `TradingKit/48`, then rewired Indicators to use shared `LIB.draw_strategy(...)`, `LIB.draw_entry_signal(...)`, and `LIB.draw_entry_default(...)`.
- 2026-03-10: Indicators entry pipeline aligned to ICT-style lifecycle rendering (`LIB.draw_entry_visual` settings parity), moved Indicators strategy stats dashboard to bottom-right, and documented strategy-specific SL->TP(RR) model with fallback guards.
- 2026-03-10: Added strategy-specific Entry planning in `Hung - Indicators.pine`: SL determined per strategy context (VWAP/EMA/BB/Ichimoku/Fib/SMA/ADX/candle/TPG), TP derived from RR, with hard sanitizer to prevent `na`/invalid plan creation.
- 2026-03-07: Stability+docs pass: removed buffer-unsafe `time[bar_index]` cutoff in HTF projected-zone ranking; synced docs for Key Levels, Liquidity/EQ, and SMC Zones to reflect current unified zone-quality and persistent keylevel rendering.
- 2026-03-07: UI perf cleanup: added local `ensure_kl_line/ensure_kl_label` helpers for PDH/PDL/PWH/PWL; guarded setter calls for activeTradingHigh/Low and SR visuals to reduce per-bar `set_*` churn.
- 2026-03-07: Minor cleanup: OB tooltips now use `LIB.zone_quality_eval` (no direct `build_zone_quality_tooltip` calls); PDH/PDL and PWH/PWL are now persistent setter-updated objects (no delete/new per bar), and keylevel line/label vars initialize to `na` (no dummy objects).
- 2026-03-07: Minor perf pass: SD zones now use `LIB.zone_quality_eval`; activeTradingHigh/Low lines/labels are updated via setters (no delete/new each bar); HTF snapshot quality computations reuse locals.
- 2026-03-07: Batch perf/token cleanup: updated SR visuals to cache `rightIdx/labelIdx`, refactored OB/RJB/FVG creation to use `LIB.zone_quality_eval`, cached `get_signal_type_idx` in `process_entries()`, and removed unused locals.
- 2026-03-07: Fixed HTF candle panel stacking (only 1 candle visible) by incrementing X offset per candle in `TradingKit.render_htf_view`; bumped import to `hunghoxuan/TradingKit/31`.
- 2026-03-07: Moved ICT helper candidates into `TradingKit/31` (zone quality scoring/tooltips/gates, candle-pattern confluence adjuster, SR visual helper, liquidity level helper) and updated ICT Zones to call `LIB.*` directly (removed `smcIsOb/smcIsFvg` wrappers).
- 2026-03-07: Moved OB/SD overlap merge helper into `TradingKit` (`try_merge_ob_sd_zone(...)`), bumped `TradingKit` publish version to `31`, and updated ICT Zones import to match.
- 2026-03-07: Shrunk `Hung - ICT SMC Zones.pine` by removing trailing inline comments on assignments and moved entry-visual rendering + signal-stats table rendering into `TradingKit` (bumped to `hunghoxuan/TradingKit/30`).
- 2026-03-07: Merged `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Dashboard.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Market Structure Levels.pine` back into `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`, reusing the existing bias/security/runtime blocks instead of duplicating them, and removed the two standalone source files from `src/`.
- 2026-03-07: Added mixed-source active-trading candidate selection in ICT Zones so `activeTradingHigh/Low` can rank local `PriceZone`, projected HTF zones, `KeyLevel` families, and prior day/week levels instead of using fallback-only logic.
- 2026-03-07: Renamed area-based type `SMC_Zone` to `PriceZone` while keeping `KeyLevel` for line-based structures.
- 2026-03-07: Added `TradingKit.KeyLevel` and migrated liquidity, EQ, and SR in ICT Zones to the line-based type while keeping area-based OB/FVG/RJB/SD/BB/iFVG on `PriceZone`.
- 2026-03-06: Replaced the active-trading-area box/band with `activeTradingHigh` / `activeTradingLow` yellow dashed key levels and reduced active-range filtering to SR only.
- 2026-03-06: Added active-trading-area visualization band in ICT Zones so the current top/bottom anchor range is visible on-chart while SR/EQ/liquidity filtering is active.
- 2026-03-06: Expanded active-trading-area filtering from SR to unified horizontal key levels (SR, EQ, liquidity), so new candidates outside the area are skipped and existing out-of-area levels are pruned.
- 2026-03-06: Added first active-trading-area filter for local SR using nearest projected HTF zone boundary with `PWH/PDH` and `PWL/PDL` fallback plus ATR distance guard; SR registration and upkeep now keep only levels inside the active area.
- 2026-03-06: Unified local horizontal key-level arrays in ICT Zones: merged liquidity, EQ, and SR bull/bear or high/low split lists into shared family arrays to reduce duplicate loops and prepare active-trading-area selection.
- 2026-03-06: Documented current-TF vs HTF zone-engine split, OB classification cost, and HTF expansion tradeoffs for lifecycle and derived zones (`BB`, `iFVG`) in feature docs.
- 2026-03-06: Added separate entry-visual toggles for unresolved/TP/SL states and introduced compact resolved-entry display mode support (`FULL_BLUR` vs `RESULT_LINES`) in the ICT entry renderer.
- 2026-03-06: Refactored ICT signals into separate `EntrySignal` (setup queue) and `Entry` (actual trade lifecycle), added `Add Entry Event` mode (`NO ENTRY` / `LIMIT` / `AFTER RETEST`), and simplified the top-right dashboard to count actual entry outcomes only by OB/BB/FVG/Sweep plus Total.
- 2026-03-05: Moved reusable signal trade-plan helpers into `TradingKit` (`get_signal_trade_plan`, `draw_signal_plan`, `prune_signal_plan_objects`), added shared settings `SIGNAL_RR_DEFAULT` and `SIGNAL_STOPLOSS`, renamed ICT `addSignal(...)` to `add_signal(...)`, and synced split imports to `TradingKit/16`.
- 2026-03-05: Added signal trade-plan visuals in ICT/SMC Zones: `addSignal(...)` now computes `entry`, `stoploss`, `takeprofit`, appends them to tooltip text, and draws compact RR boxes plus entry line on chart.
- 2026-03-05: Removed remaining duplicated `get_lib_trend_data(...)` wrappers from Zones and Dashboard scripts by switching to direct `LIB.get_trend_data(...)` call sites.
- 2026-03-05: One-pass dedupe: added `TradingKit.get_htf_pair()`, replaced local `htfList->htf1/htf2` boilerplate in all 3 `Hung - *.pine` files, removed legacy `ConflunceResult` alias type, and synced imports to `TradingKit/15`.
- 2026-03-05: UI cleanup pass (Zones): standardized Buy/Sell input labels and compacted signal/zone tooltip phrasing for consistent, minimal UX.
- 2026-03-05: Performance pass (Zones): added hot-loop object-update guards in `_controlZone`, `update_liquidity`, and SR rendering (`set_right`/`set_x2` only on change).
- 2026-03-05: Performance pass (Market Structure): added object-reuse guards in HTF mini-candle and HTF label update loops to reduce redundant setter calls.
- 2026-02-26: HTF Candles Overlay
- 2026-02-26: HTF Bias Dashboard (weighted)
- 2026-02-26: Killzones
- 2026-02-26: Liquidity Sweeps (BSL/SSL)
- 2026-02-26: Trend Lines
- 2026-02-26: EQH/EQL detection and break state
- 2026-02-26: Key Levels (PDH/PDL, PWH/PWL)
- 2026-02-26: Divergence (RSI/MACD/OBV)
- 2026-02-26: SMC zones (OB/FVG/RJB)
- 2026-02-26: HVB + PPDD + stacked OB/FVG markers
- 2026-02-26: VWAP/RSI trigger icons
- 2026-02-27: BOS/MSS visual semantics restored to legacy behavior
- 2026-02-27: Unified detection framework added (state/cross + wick/close/body + impulse)
- 2026-02-27: Unified detection framework validated by user in TradingView
- 2026-02-27: Liquidity sweep visibility restored (legacy wick sweep trigger + explicit sweep markers)
- 2026-02-27: OB/FVG retest pending signal flow restored in unified zone controller
- 2026-02-27: Bias confluence engine phase-1 implemented (factor toggles + min score + current TF signal filter)
- 2026-02-27: Dashboard upgraded with current-TF score tile and detailed value-aware bias tooltips
- 2026-02-27: Bias confluence phase-2 added (EMA20/50, MACD zero, BB-walk optional factors)
- 2026-02-27: Signed score model applied to Trend/Bias and Buy/Sell confluence tooltips
- 2026-02-27: Weighted confluence scoring and simplified settings UX implemented
- 2026-02-27: Added profile-driven thresholds and shared Buy/Sell score gate for entry creation
- 2026-02-27: Added Supertrend/Donchian/Heikin-Ashi bias factors with settings + tooltip support
- 2026-02-27: Reduced dashboard argument plumbing (global-series based) to lower Pine token pressure
- 2026-02-27: Published exact multi-indicator split map (3-file primary + 2-file fallback)
- 2026-02-28: Created 2-file split phase-1 baselines in `src/` + shared-contract document
- 2026-02-28: Applied non-overlap hotfix in core file by hard-disabling bias/signal/aux outputs
- 2026-02-28: Option-2 applied: Bias Dashboard + HTF candles moved to core file, disabled in merged bias/signal/aux file
- 2026-02-28: Per-file settings cleanup: removed unrelated input groups from each split file
- 2026-02-28: Trendline ownership moved to merged bias/signals file (disabled in core)
- 2026-02-28: Physical prune pass-1 completed (dashboard/HTF removed from merged file; trendline execution removed from core)
- 2026-02-28: Physical prune pass-2 completed in `ict_bias_signals_aux.pine` (residual SMC zone/liquidity/EQ engines and dead SMC settings removed)
- 2026-02-28: Physical prune pass-A completed in `ict_smc_structure_zones.pine` (divergence and buy/sell auxiliary execution removed)
- 2026-02-28: Optimization batch completed on split files (dead constants/functions removed, global-arg simplification applied, unused HTF reads/scaffolding removed)
- 2026-02-28: Added weak-zone quality filter for OB/FVG/RJB creation (relative volume + impulse body/ATR threshold)
- 2026-02-28: Added OB-priority de-duplication rule (skip RJB when overlapping/nearby OB exists)
- 2026-02-28: Added reusable zone quality scoring model and surfaced score details in OB/FVG/RJB tooltips
- 2026-02-28: Added optional HTF OB/FVG projection on current chart with fixed object budget (4 reusable boxes)
- 2026-02-28: Refactored zone model to `type + isBullish + labelText`, moved tooltips onto visible zone labels, and switched zone fill to neutral background
- 2026-02-28: Added HTF projected-zone quality threshold + optional quality % text, reusing shared zone quality model
- 2026-02-28: Improved HTF projection mapping/extension and label semantics (real TF text + directional colors), and unified quality % option across HTF/current zones
- 2026-02-28: Replaced static zone extension with dynamic extension factors (timeframe + type + zone power) and removed quality % from zone labels
- 2026-02-28: Enforced strict HTF extension floor so HTF zones always extend farther right than current-TF zones
- 2026-02-28: Switched HTF projection rendering to bar-index mapping for reliable visible extension and added dotted gray HTF borders
- 2026-02-28: Added extension-factor inputs and improved mitigated-zone styling (darker neutral fill + neutral border/label)
- 2026-02-28: Added strongest-zone ranking caps (per type/side + total) and weakest-first pruning for active zone retention
- 2026-02-28: Restored iFVG and Breaker Block (BB) lifecycle transitions in unified zone engine
- 2026-02-28: Added BB/iFVG retest marker signals in unified zone lifecycle
- 2026-02-28: Restored unified ICT/SMC Buy/Sell signal pipeline for OB/FVG/RJB/BB/iFVG (pending + confirmation + confluence/toggle settings)
- 2026-02-28: Restored legacy-style Buy/Sell settings block labels and signal marker format (score-only potential/confirmed + sweep toggle wiring)
- 2026-02-28: Added alertcondition hooks for potential/confirmed/sweep signal events with BUY/SELL SIGNALS alert toggles
- 2026-02-28: Added shared `webhook_alert()` method and optional JSON webhook emission toggle in ICT/SMC signal pipeline (method parity with all-indicators file)
- 2026-02-28: Reworked HTF candle overlay to render 3 side-by-side panels from `biasTf1/2/3` with fixed 10-bar spacing and TF labels below each panel
- 2026-02-28: HTF candle panels now dedupe and sort low→high timeframe across `HTF Candles + Bias1/2/3`, with configurable W/D/4h/Others candle counts
- 2026-02-28: Restored compact 3-slot HTF mini-chart logic with ordered source filtering (`HTF Candles -> Bias1 -> Bias2 -> Bias3`), dedupe, and fixed default counts (16/12/7/3)
- 2026-02-28: Added lightweight OB class tagging in existing OB loop (`O` Original, `D` Decisional, `X` Extreme) without new OB arrays
- 2026-02-28: Aggressive token-trim pass applied (optional bias factors removed, webhook runtime removed, HTF counts converted to constants, dashboard/tooltips simplified to core factors)
- 2026-02-28: Additional token trim: removed dead helper paths and compressed bias tooltip output strings (same scoring, shorter payload text)
- 2026-02-28: Additional string-budget trim: shortened zone/BOS/MSS/EQ/PDH-PDL tooltips and compacted quality tooltip formatting
- 2026-02-28: Dashboard token trim: removed HTF detailed factor/security payload and switched HTF dashboard tooltips to compact summary
- 2026-02-28: Trend payload trim: removed unused trend flags bitmask and simplified dashboard strength color mapper to active 3-state inputs only
- 2026-02-28: Micro trim: inlined compact HTF dashboard tooltip and removed single-use helper
- 2026-02-28: Cleanup trim: converted advanced detection settings to constants, removed identity label helper, simplified quality tuple reads, and removed obsolete inline commented-input remnants
- 2026-03-01: Settings trim: switched to checkbox-only inputs (all non-boolean settings converted to constants)
- 2026-03-01: Tooltip payload trim: centralized buy/sell reason builders and compacted signal/zone tooltip text payloads
- 2026-03-01: Forced selected toggles to constants (`Show Bias Table`, `Show Auxiliary Icons`, `Show Potential`, `BB/iFVG Retest Icons`, `Use Vol Filter`, `Use Impulse Filter`, `HTF Zones`)
- 2026-03-01: Added lightweight OB subtype detection and labeling (`Origin`, `Decisional`, `Extreme`, `Mitigation`)
- 2026-03-01: Added one-time zone touch+respect detection signal for mitigated zones
- 2026-03-01: Added candle-pattern enum pipeline (`get_candle_pattern`) and confluence score supplement for Retest/Sweep/Respect/Confirm events
- 2026-03-01: Upgraded candle pattern engine with safe wick-ratio thresholds and directional doji classes (dragonfly/gravestone)
- 2026-03-01: Refined extended pattern set quality (ATR-tolerant tweezers, stricter 3-candle patterns, practical marubozu) and completed mapping for confluence usage
- 2026-03-01: Added compact candle-volume confluence bonus (RV20) to event scoring pipeline
- 2026-03-01: Added temporary candle confluence debug panel for fast threshold tuning
- 2026-03-01: Removed temporary candle confluence debug panel to reclaim token budget after tuning pass
- 2026-03-01: Synced candle-pattern documentation with live code (pattern list, precedence, bias map, confluence contribution rules, limitations)
- 2026-03-01: Replaced zigzag swing-circle label rendering with shared plotchar-based circle pipeline (major/minor bull/bear series)
- 2026-03-01: Fixed swing-circle rendering to plot only on new pivot events and switched to clean `plotshape(shape.circle)` markers (removed blob effect)
- 2026-03-01: Switched swing-circle renderer to `plot.style_circles` for reliable non-label, price-level circle display
- 2026-03-01: Replaced swing-circle renderer with sample-aligned `plotchar(... location.absolute, offset=-PP)` for clean pivot-candle circles
- 2026-03-01: Aligned swing circles to tracker-classified pivots (major/minor index matching) so major/minor circles map to BOS/MSS structure tiers
- 2026-03-01: Refined structure core: unified minor BOS/MSS break checks to `ta.crossover/crossunder` (same as major) and relaxed minor activation gate from rigid `m,m,M` to recent minor-pair detection
- 2026-03-01: Unified circle source with classified `ArrayTypeAdv` pivot stream and removed legacy in-method circle state path
- 2026-03-01: Added resilient circle fallback gating (major/minor index + classified stream + pivot fallback) to prevent missing swing circles
- 2026-03-01: Circle semantics normalized to pivot-time classification and removed minor-toggle side effect from major emit path
- 2026-03-01: Circle rendering simplified to explicit index-only classification (major/minor by tracker indices, no mixed fallback stream)
- 2026-03-01: Added temporary swing-class text markers (`M`/`m`) on pivot circles for visual verification of major/minor classification
- 2026-03-01: Added faint raw-pivot fallback circles so unclassified confirmed pivots remain visible between classified major/minor swings
- 2026-03-01: Added display-only micro-pivot fallback (`len=3`) to reveal missing local valleys/peaks without changing structure/BOS/MSS engine
- 2026-03-01: Added reversal-based fallback dots (1-bar valley/peak) to visualize immediate local turns missed by pivot-window confirmation
- 2026-03-01: Added high-visibility swing fallback markers plus detector debug tags (`P/p/v`) for deterministic pivot-detection diagnosis
- 2026-03-01: Cleaned swing debug noise: disabled debug labels by default and reduced fallback markers to tiny/low-opacity with feature toggles
- 2026-03-01: Tuned swing fallback to low-focused mode (fallback highs off, reversal-valley low emphasized) to surface missing troughs without high-side clutter
- 2026-03-01: Rollback applied: reverted swing-visual refactor attempts due runtime timeout; parked major/minor swing reclassification feature for later
- 2026-03-01: Runtime safety fix: bounded variable historical offsets in EQH/EQL validation loops to prevent TradingView buffer overflow runtime errors
- 2026-03-01: Runtime safety fix: clamped `_controlZone` label `x` updates to safe bar-index window to avoid historical-offset overflow at `visualLabel.set_x(...)`
- 2026-03-04: Merged zone touch-respect into unified pending->confirmed zone-hold pipeline and reordered execution to detect->queue->confirm in same bar
- 2026-03-01: Added major-structure invariant normalization: promote interval extrema between consecutive major highs/lows
- 2026-03-01: Added major-pivot backfill dots at exact promoted major indices to visualize retroactive major normalization points
- 2026-03-01: Fixed major normalization scope from latest-pair only to all consecutive major-high/major-low pairs
- 2026-03-01: Restored explicit minor-high visibility (independent toggles + stronger minor-high contrast) while keeping fallback-high suppression
- 2026-03-01: Added minor-pivot backfill dots at exact `Minor_HighIndex`/`Minor_LowIndex` to render retroactive minor classification updates
- 2026-03-01: Added bar-range major interval extrema enforcement and re-enabled high-side fallback visibility; boosted minor-high marker visibility
- 2026-03-01: Increased backfill marker visibility and added dedicated minor-high fallback stream for consistent high-side minor swing rendering
- 2026-03-01: Replaced noisy mixed swing rendering with a deterministic draw-once renderer (major interval-extrema + classified minor nodes)
- 2026-03-01: Recovery patch after clean refactor: removed draw lookback gate and added baseline raw minor high/low plotchar visibility fallback
- 2026-03-05: Final settings/type cleanup pass: switched zone/structure files to TradingKit v14, removed local `GLOBAL_MAX_LOOKBACK_BARS` and `HTF_CANDLES_*` aliases in critical paths, and migrated local `HTF_Data` to `LIB.HTF_Data`
- 2026-03-05: Alias cleanup pass: removed remaining Settings-based alias constants in Zone/Structure files and switched all affected runtime paths to direct `SETTINGS.*` access
- 2026-03-05: Performance pass: enabled `dynamic_requests` in Zones/Structure indicators, replaced duplicated static HTF wrappers with direct HTF1/HTF2 security calls, and removed dead wrapper methods
- 2026-02-27: Docs structure migrated (`ai-agent` reusable, `docs` project-specific)
- 2026-02-27: Documentation finalized with index files and normalized reference naming

## Upcoming Features
1. Non-semantic performance optimization batch (safe, incremental).
2. Optional BOS/MSS debug counters for parity checks.
3. Optional unified pending-entry pipeline (if requested).
4. Alert conditions (BOS/MSS, sweep, zone events).
5. Additional key-level categories (if requested): session highs/lows, monthly levels, custom S/R.
6. Bias Confluence Checkbox Engine (configurable factor toggles + score thresholds).

## Current Validation Baseline
- Working backup:
`/Users/macmini/Trade/Bot/Hung Bot/backups/_2_ICT-SMC-PA All-in-one2.pine.bak_20260227_085000_v1.1.0_unified_detection_validated_working`

## Full Completed Steps So Far (Tracking)
- 2026-03-05: Refactored signal trade-plan logic into `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` for reuse by other indicators, introduced shared settings `SIGNAL_RR_DEFAULT` and `SIGNAL_STOPLOSS` (0 = auto zone-edge stop, >0 = ATR-multiple stop), renamed local ICT emitter to `add_signal(...)`, and synchronized all split-indicator imports to `TradingKit/16`.
- 2026-03-05: Extended `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` signal rendering so every emitted signal now derives `entry/SL/TP` from the zone or point event, then draws a capped compact risk-reward visual (`TP box`, `SL box`, `entry line`) directly on chart.
- 2026-03-05: Completed remaining duplication cleanup by removing local `get_lib_trend_data(...)` wrappers from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Dashboard.pine`, replacing usage with direct `LIB.get_trend_data(...)`.
- 2026-03-05: One-pass dedupe completed across split scripts: centralized HTF pair extraction in `TradingKit.get_htf_pair()`, removed local HTF list parsing in all `Hung - *.pine` files, removed `ConflunceResult` alias from library types, and bumped all split imports to `TradingKit/15`.
- 2026-03-05: Completed UI cleanup pass in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` (minimal/consistent Buy-Sell input naming and compact unified tooltips).
- 2026-03-05: Completed performance pass in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` (hot-loop guards to avoid redundant `set_right`/`set_x2`/label text-position updates).
- 2026-03-05: Completed performance pass in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Market Structure Levels.pine` (HTF candle/label object-reuse guards to reduce repeated setter calls).
1. Unified detection framework implemented and user-validated.
2. Documentation split into project docs (`docs/`) and reusable agent docs (`ai-agent/`).
3. Reference indicator/strategy scripts moved under `docs/references/`.
4. Deprecated `versions/` usage removed; legacy file migrated into `backups/`.
5. Feature docs refreshed to match latest source in `src/`.
6. Reference file naming normalized to consistent `lowercase_snake_case` under `docs/references/` (including indicators/strategies).
7. Added docs index files (`docs/features/INDEX.md`, `docs/references/INDEX.md`, `docs/schedule/INDEX.md`) and finalized cross-links.
8. Completed non-semantic performance batch #1: added global draw lookback throttle (`perfLookbackBars`) and gated high-frequency label/icon rendering paths (signals, divergence labels, trendline tooltips, PPDD/stack markers, VWAP/RSI icons) without changing BOS/MSS core semantics.
9. Added traceability/documentation rule: latest source-file trace header comment (timestamp/update/features/last-backup) + major-update conversation summaries under `ai-agent/conversations/`.
10. Added optional BOS/MSS parity debug counters with on-chart table (`showStructureDebug`, `structureDebugWindowBars`) showing major/minor BOS/MSS totals and rolling-window counts.
11. Cleaned inputs and removed temporary structure debug feature; moved global controls to `Settings` and grouped EQH/EQL + Key Levels under `ICT/SMC` with concise inline rows.
12. Restored Buy/Sell signal label visibility by decoupling `addSignal(...)` from `showAuxiliaryIcons` (hotfix).
13. Fixed liquidity sweep signal regression by restoring legacy close-based sweep confirmation and increasing line budget to reduce missing liquidity lines.
14. Restored liquidity sweep detection to strict legacy wick-state checks (`high > level` / `low < level`) and added explicit on-sweep marker icons for immediate chart visibility.
15. Restored OB/FVG retest pending signal emission via unified `_controlZone` using shared detection methods (`check_touch`, `check_mitigation`, `check_break`) and existing confirmation pipeline (`checkConfirmations`).
16. Fixed Pine compile-order errors in unified OB/FVG retest block by removing forward references before helper declarations (inline pending dedupe + local score literals).
17. Implemented reusable bias confluence phase-1: new `Bias` group factor toggles/min score, dynamic bias power normalization, and unified HTF+current-TF bias alignment in signal gating.
18. Enhanced bias dashboard UX: current-TF cell now displays score (`Sx/max`) with strength color; tooltips now show real factor values (RSI/ADX/EMA/VWAP/structure) plus learning-note constants.
19. Implemented bias confluence phase-2 with reusable factor-data method (`get_bias_factor_values`) and optional extra factors (EMA20/50, MACD zero, BB-walk + params), wired into both scoring and tooltip display.
20. Implemented signed confluence scoring (`+1/-1/0`) for Trend/Bias and Buy/Sell components, updated dashboard cells to show total/max->% and per-indicator score explanations with live values.
21. Upgraded to weighted confluence model with factor-weight labels in settings, auto threshold (removed manual min score), simplified BB walk settings, and added tunable factor/trigger ranges.
22. Added `Confluence Profile` presets (Scalp/Intraday/Swing), compact score-in-parentheses tooltips, and unified score-gate usage between dashboard confluence and OB/FVG retest pending entry generation.
23. Added alternative single-timeframe bias factors (Supertrend, Donchian, Heikin-Ashi) into weighted confluence engine with default effectiveness ordering and HTF tooltip propagation.
24. Reduced token-heavy dashboard plumbing by refactoring `create_dashboard_bias` to consume existing global timeframe/bias series and trimming unused bias tooltip parameters.
25. Added exact function/type/state allocation plan for splitting into 3 indicators with minimal dependency (`docs/features/MULTI_INDICATOR_SPLIT_PLAN.md`).
26. Created 2-file split phase-1 baseline files and documented strict shared method/constant/variable contract (`docs/features/TWO_FILE_SHARED_CONTRACT.md`).
27. Enforced operational non-overlap in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc_structure_zones.pine` by hard-disabling dashboard/killzones/HTF-candles/divergence/signal overlays.
28. Applied Option-2 wiring: re-enabled dashboard and HTF candle overlay in core file, and disabled both in merged bias/signals/aux file.
29. Completed settings-level split cleanup: `ict_bias_signals_aux.pine` no longer exposes `Market Structure`/`ICT-SMC` inputs; `ict_smc_structure_zones.pine` no longer exposes `Buy/Sell`/`Indicators` input groups.
30. Reassigned trendline ownership: restored trendline input controls in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_bias_signals_aux.pine` and hard-disabled trendline settings in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc_structure_zones.pine`.
31. Executed physical code prune: removed `create_dashboard_bias` + HTF-candle overlay blocks from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_bias_signals_aux.pine`, and removed `execute_trend_lines` execution block from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc_structure_zones.pine`.
32. Executed physical code prune pass-B in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_bias_signals_aux.pine`: removed remaining `PriceZone` state/functions, duplicate `ms_tracker` trigger path, and dead SMC-only input groups/helpers.
33. Executed physical code prune pass-A in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc_structure_zones.pine`: removed divergence module, removed buy/sell auxiliary execution chain, simplified dashboard to bias-only tiles, and simplified zone controller invocation to core-only behavior.
34. Executed optimization batch across `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc_structure_zones.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/ict_bias_signals_aux.pine`: removed dead constants/functions/inputs, simplified `_controlZone` and liquidity/EQ flows toward global state, and removed unused HTF/security reads and obsolete confluence scaffolding in bias file.
35. Implemented weak-zone quality gating in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` using reusable `passes_zone_quality(...)` (size + relative volume + impulse body/ATR).
36. Implemented OB-priority RJB de-duplication in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` using reusable overlap/nearby checks (`zones_overlap(...)`, `has_nearby_ob(...)`).
37. Implemented reusable zone quality scoring in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`get_zone_quality_data(...)`, `build_zone_quality_tooltip(...)`) and wired score/metrics into OB/FVG/RJB tooltips.
38. Implemented optional HTF zone projection in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` via fixed-size reusable overlays (`get_htf_zone_snapshot(...)`, `update_projected_box(...)`) and new HTF zone settings toggles.
39. Refactored zone semantic model in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`PriceZone.type`, `PriceZone.isBullish`, `PriceZone.labelText`), simplified type checks, and moved OB/FVG/RJB tooltip ownership to visible labels with directional placement rules.
40. Added projected HTF zone quality gating and score text in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` by extending HTF snapshot payload with quality percent and applying `HTF Min Quality %` threshold before rendering.
41. Updated HTF projection in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to map left edge from HTF source candle time, extend further right via configurable bars, show timeframe-specific labels (`4h OB/FVG`), use directional label/border colors, and apply quality % label option consistently on both projected and local zones.
42. Implemented shared dynamic extension model in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`tf bonus + type bonus + power bonus`) for both local and HTF zones, persisted per-zone extension via `PriceZone.extendBars`, removed `%` from all zone labels, and improved HTF label clarity with consistent directional text/border styling.
43. Added hard HTF extension guard in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` so computed HTF extension is always greater than max possible local extension by a fixed margin.
44. Updated HTF draw pipeline in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to resolve HTF source time into chart `bar_index` for left anchor and draw with `xloc.bar_index`, ensuring right-extension visibility; set HTF borders to dotted neutral gray as requested.
45. Added tunable extension settings in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (HTF bonus, OB/RJB type bonus, HTF-over-local floor, power thresholds/max) and updated mitigated-zone visuals to use darker neutral background with neutral border/label colors for clearer state distinction.
46. Implemented ranking-based zone retention in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: each new OB/FVG/RJB stores `qualityPct`, then `add_active_zone(...)` prunes weakest zones by bucket (`type + side`) and enforces a global total cap.
47. Reintroduced `iFVG` and `BB` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` using derived-zone transitions (`spawn_derived_zone(...)`) from broken `FVG` and `OB` respectively, aligned with unified zone type/state methods and ranking/extension controls.
48. Added unified BB/iFVG retest signal markers in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` on first mitigation touch, using existing zone lifecycle state instead of legacy parallel signal handlers.
49. Restored generalized ICT/SMC signal engine in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with `entrySignal` pending queue, `addSignal(...)`, per-zone-type toggles (OB/FVG/RJB/BB/iFVG), optional HTF-bias filter, and confluence score gating/confirmations on zone retest.
50. Updated signal UX and controls in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: confirmed markers now arrow+colored background with score-only text, potential markers score-only text without arrow/brackets, and restored `BUY/SELL SIGNALS` checkbox block including `Sweep Liquidity`.
51. Added unified alert events in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: per-bar signal event flags, alert toggles, and `alertcondition(...)` outputs for potential/confirmed/sweep buy/sell events.
52. Added common `webhook_alert(...)` method in both `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/all-indicators [hung].pine`; wired optional JSON webhook dispatch from unified signal labels in ICT/SMC core.
53. Replaced the legacy single HTF mini-chart in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with 3 HTF mini-charts bound to `biasTf1`, `biasTf2`, `biasTf3`, arranged next to each other with 10 bars gap and per-panel timeframe label under each chart.
54. Updated HTF mini-chart composition in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to build unique timeframe set from (`globalHtfString`, `biasTf1`, `biasTf2`, `biasTf3`), sort from low→high TF, and apply configurable candle counts (`4h=12`, `D=7`, `W=3`, `Others=16` defaults).
55. Reverted to stable token-safe baseline and reimplemented HTF panels in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with exactly 3 slots, source-order filtering, dedupe, `> HTF Candles TF` bias gating, and per-source count defaults (`16/12/7/3`) plus TF labels under each panel.
56. Added OB-type classification tags in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` using current `activeZones` loop only (no extra arrays): `O` (first same-side active OB), `D` (quality >= 70), `X` (extreme side candidate), rendered on OB label/tooltip.
57. Per user rollback + optimization focus: restored pre-OB-tag baseline, then reduced compile pressure in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` by removing EMA-cross/MACD/BB-walk/Heikin bias branches, removing webhook JSON runtime path, converting HTF panel counts to constants, and simplifying dashboard factor plumbing to core bias factors only.
58. Applied additional low-risk trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed unused `calc_zigzag_inputs`/HTF security call and unused tooltip-icon helper, and shortened `get_bias_tooltip_detailed(...)` text lines to cut string-token footprint.
59. Applied tooltip string compaction in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: shortened OB/FVG/RJB/EQ/key-level/BOS/MSS descriptions and compressed `build_zone_quality_tooltip(...)` output to compact metric format.
60. Applied dashboard payload trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed HTF `get_bias_factor_values(...)` + HTF close `request.security(...)` reads, limited detailed factor tooltip/scoring to current TF cell, and added compact HTF tooltip (`Bias + Strength + Flag`) for higher timeframes.
61. Applied trend payload trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed unused `flags` bitmask from `get_trend_data(...)` return path and dashboard arrays, reduced compact HTF tooltip to `Bias + Strength`, and simplified `get_strength_color(...)` to current 3-state (`bull/bear/neutral`) mapper.
62. Applied micro token trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed single-use `get_bias_tooltip_compact(...)` helper and inlined compact HTF tooltip text generation in dashboard cell render path.
63. Applied cleanup trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: replaced advanced detection `input.*` controls with constants at existing defaults, removed `build_zone_label_text(...)` identity helper and direct-called label text, compacted tuple destructuring in zone quality helpers, and removed stale comment-heavy pseudo-input remnants for fixed BOS/MSS style constants.
64. Applied settings-model trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: converted all non-boolean `input.*` settings (timeframes/strings/ints/floats/colors/styles/sizes) to fixed constants, leaving only `input.bool(...)` checkbox toggles configurable.
65. Applied tooltip payload trim in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: shortened global tooltip prefixes (`Buy setup`/`Sell setup`, `R:`), added reusable `tt_buy(...)`/`tt_sell(...)` builders, migrated repeated tooltip concatenations to helpers, compacted `addSignal(...)` tooltip keys (`S/Z/Q`), and removed one unused temporary tooltip string in zigzag marker labeling.
66. Applied requested fixed-toggle policy in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: converted specific bool settings to constants with exact values: `showBiasTable=true`, `showAuxiliaryIcons=true`, `signalShowPotential=true`, `signalBbIfvgRetest=false`, `smc_filter_use_volume=true`, `smc_filter_use_impulse=true`, `showHtfProjectedZones=true`.
67. Added OB subtype lifecycle in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: new OB class helpers (`Origin/Decisional/Extreme/Mitigation`), OB creation now labels as `OB-O/D/X` using active-zone context, and mitigated OBs auto-switch label to `OB-M` with mitigation tooltip on first mitigation event.
68. Added zone respect detection in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: after mitigation, zones now emit a one-time confirmed signal when price touches the zone and closes back in the expected direction (`touch respected`), with per-zone dedupe via `z.barEnd`.
69. Added candle-pattern confluence module in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: implemented enum-style `get_candle_pattern()` + text/bias helpers, and applied pattern-aligned score bonus/annotation to signal pipeline for zone retests, pending confirmations, liquidity sweeps, and zone-respect confirmations.
70. Enhanced `get_candle_pattern()` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` using wick-ratio quality logic: introduced safe denominator handling, configurable ratio thresholds (`CP_*` constants), directional doji classes (`Dragonfly`/`Gravestone`), and updated pattern text/bias mapping for stronger confluence filtering.
71. Refined user-extended patterns in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: completed `get_candle_pattern_text()`/`get_candle_pattern_bias()` coverage for newly added enums, replaced mintick-only tweezer tolerance with ATR-relative tolerance, tightened `3 White Soldiers`/`3 Black Crows` with body/wick/open-sequencing quality checks, and relaxed marubozu detection to practical wick/body thresholds.
72. Added volume confluence in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with minimal token overhead: within existing `apply_candle_pattern_confluence(...)`, signals now get `+1` score when `relVolume(20) >= 1.2` on confluence events, with optional `+ Vol` reason suffix.
73. Added compact temporary debug panel in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`DEBUG_CANDLE_CONFLUENCE=true`) showing live candle-confluence internals (`pattern`, `relVol`, `bonus`, `aligned`, `isEvent`) to speed manual threshold calibration.
74. Removed the temporary candle confluence debug panel from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` after initial calibration to reduce compile-token usage while preserving the confluence scoring logic.
75. Synced `/Users/macmini/Trade/Bot/Hung Bot/docs/features/CANDLE_PATTERN_CONFLUENCE.md` to current implementation with exact supported patterns, bias mapping, scoring conditions, and limitation notes.
76. Replaced swing circle drawing in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` from `label.new(...SYM_DOT...)` to shared `plotchar` series rendering via `circle_plot_value(...)`.
77. Corrected swing-circle visual behavior in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` by gating marker emission to new pivot index only (`CircleLastPivotIndex`) and replacing glyph-based `plotchar` output with transparent `plotshape(shape.circle)` markers.
78. Replaced `plotshape` circles with `plot(..., style=plot.style_circles)` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to ensure circles render consistently at pivot prices.
79. Replaced circle rendering in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with direct `plotchar` (absolute + `offset=-PP`) on confirmed pivot highs/lows to match requested visual behavior.
80. Updated circle emit conditions in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to use `ms_tracker.Major_*Index` and `ms_tracker.Minor_*Index` classification instead of raw `hp/lp` only; minor circles are suppressed when same pivot is promoted to major.
81. Updated major/minor consistency in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: minor break triggers now mirror major (`ta.crossover` / `ta.crossunder`) and minor-level setup now activates on trailing minor pair (`m*`,`m*`) instead of requiring a third `M*` predecessor.
82. Updated pivot-circle emission in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to draw from latest classified `ArrayTypeAdv` pivot (major/minor + high/low) keyed by pivot index (`hi/li`), and removed obsolete circle-update block from `process_zigzag(...)`.
83. Hardened pivot-circle render conditions in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with fallback emit logic so circles remain visible even when strict classified-index matching does not fire on a given confirmed pivot.
84. Removed `or not zigzagShL_m` branch from major circle emit rules in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` so major circles are independent from minor toggle state and represent pivot-time classification only.
85. Replaced mixed fallback circle emit logic in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with explicit index-only mapping: major circles use `Major_*Index`; minor circles use `Minor_*Index` excluding major overlap.
86. Added temporary pivot classification overlay in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: `plotchar` text markers (`M` for major, `m` for minor) at swing points to verify structure-class assignment on chart before final cleanup.
87. Added fallback pivot render in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: faint `SYM_DOT_TINY` at confirmed `hp/lp` not classified as current major/minor, improving visual continuity of zigzag turns.
88. Added visual micro-pivot fallback in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`VIS_PIVOT_LEN=3`) with faint dots (`Micro Pivot High/Low`) to surface local swing points that are not confirmed under main `PP=5`.
89. Added additional visual-only reversal fallback in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`Reversal Valley/Peak`) using 1-bar turn detection (`low[1] < low[2] and low[1] <= low`, mirrored for highs) to reveal missing in-between turns.
90. Added temporary diagnostic overlay in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: boosted fallback marker visibility and added detector labels (`P` = main pivot, `p` = micro pivot, `v` = reversal) at absolute turn bars to identify whether missing swings are due to detection or rendering.
91. Applied swing-visual cleanup in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: set debug overlays off by default (`DEBUG_SWING_CLASS_TEXT=false`, `DEBUG_SWING_DETECT_MARKERS=false`), added fallback toggles (`SHOW_SWING_FALLBACK_RAW/MICRO/REVERSAL`), and restored fallback marker style to tiny/transparent to preserve major/minor readability.
92. Refined fallback visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: introduced `SHOW_SWING_FALLBACK_HIGHS=false` and tuned low-side fallback markers (especially reversal valleys) to improve missing-swing-low visibility while preventing excessive high-side dots.
93. Reverted `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to `/Users/macmini/Trade/Bot/Hung Bot/backups/ict_smc [hung].pine.bak_20260301_170839_low_only_fallback_cleanup` after runtime timeout (`script takes too long to execute`) from subsequent swing-visual refactor iterations; feature put on hold.
94. Applied runtime guard in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `SAFE_DYNAMIC_OFFSET=4000` and bounds checks around dynamic `high[checkOff]`/`low[checkOff]` indexing in `find_equal_high` / `find_equal_low` loops to avoid `requested historical offset ... beyond historical buffer` errors.
95. Applied `_controlZone` draw-coordinate guard in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `SAFE_LABEL_BACK_BARS=4000` and clamped label `x` (`labelXRaw -> [bar_index-4000, bar_index+500]`) before `z.visualLabel.set_x(...)` to prevent runtime historical-offset errors originating from stale/old zone coordinates.
93. Implemented major interval-extrema normalization in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: between last two major highs, the minimum low pivot is promoted to major; between last two major lows, the maximum high pivot is promoted to major. Added helper `to_major_type(...)` for safe `m* -> M*` promotion.
94. Added major backfill-render path in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: when `Major_HighIndex`/`Major_LowIndex` changes (including retroactive promotion), draw a one-time dot label at that historical `bar_index` to keep major swing visuals aligned with normalized structure.
95. Corrected major invariant algorithm in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: it now iterates through every consecutive major-high pair and major-low pair, promoting interval extrema for each pair (instead of only handling the latest pair).
96. Added `SHOW_MINOR_SWING_HIGHS/SHOW_MINOR_SWING_LOWS` toggles and increased minor-high marker contrast in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` so minor highs remain visible even with `SHOW_SWING_FALLBACK_HIGHS=false`.
97. Added minor backfill-render path in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: draw one-time dot labels at `Minor_HighIndex`/`Minor_LowIndex` when they change, avoiding `plotchar offset` timing loss for retroactive minor pivot updates.
98. Added interval-extrema enforcement from raw bar data in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: when a new major high/low is confirmed, scan bars between consecutive major pivots to set opposing major level/index to true extrema; re-enabled `SHOW_SWING_FALLBACK_HIGHS` and increased minor-high plot contrast/size.
99. Raised backfill marker prominence in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (`SIZE_SMALL`, lower transparency) for major/minor backfill dots and added `Minor Swing High Fallback` plot path (`hasPivotHigh` based) so minor highs remain visible when strict minor-index mapping does not fire.
100. Rewrote the swing visualization section in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed layered fallback/debug plotchar paths, added helper methods (`has_drawn_idx`, `draw_once_point`), and now render swings via draw-once labels from (a) major interval extrema between consecutive major highs/lows and (b) classified `m*` minor nodes within lookback.
101. Recovery update in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed `perfLookbackBars` gate from draw-once swing labels (to restore historical visibility) and added raw pivot baseline plots (`Minor Raw High/Low Baseline`) so minor highs/lows are always visible even if classified-node rendering lags.
102. Improved minor-high visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: minor swing plot gate now resolves latest minor high/low indices from `ArrayTypeAdv/ArrayIndexAdv` history (`get_latest_minor_high_index`, `get_latest_minor_low_index`) instead of relying only on mutable `Minor_*Index` state.
103. Hardened minor-high backfill rendering in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: minor-high label draw now reads latest `mH` index/value directly from `ArrayTypeAdv/ArrayIndexAdv/ArrayValueAdv`, preventing missed plots when `Minor_HighIndex` state is stale.
104. Reworked HTF projected-zone rendering in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: dedupes HTFs from (`HTF Candles`, `Bias1`, `Bias2`, `Bias3`) above current timeframe and, per HTF, renders only nearest closed zones (`2 above + 2 below`) with fixed constants (`HTF_ZONE_PER_SIDE`, `HTF_ZONE_PER_TF`, `HTF_ZONE_MAX_TF`).
105. Updated HTF projected-zone constraints in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: capped projected HTFs to `<= D` (excluding `W`/`M`), enforced extra extension bonus for `D` zones over `4h`, and excluded mitigated candidates from the `2 above + 2 below` per-HTF display filter.
106. Refined current-TF zone label placement in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: centered labels vertically (`ZONE_LABEL_CENTER_BIAS=0.0`), increased right padding (`ZONE_LABEL_RIGHT_PAD=2`), and forced runtime label text alignment to right (`set_textalign(text.align_right)`) for consistent in-box positioning.
107. Replaced liquidity source in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` from standalone pivot (`liquidity_len`) lines to structure-derived levels (`Major_High/Low`, `Minor_High/Low`), with deduped swing-index registration and line lifecycle in `xloc.bar_index`.
108. Upgraded sweep logic in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to require close-back/rejection confirmation (`check_zone_respect`) before sweep signal emission, and added LDM sequence confirmation (`sweep+rejection` then BOS/MSS shift within `LDM_CONFIRM_BARS`) with new score component `SCORE_LDM`.
109. Rolled back `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to `/Users/macmini/Trade/Bot/Hung Bot/versions/ict_smc [hung].pine.ver_20260301_221654_before_liquidity_swing_ldm` due TradingView compile token overflow (`80543 > 80000`) after liquidity/LDM expansion.
110. Removed legacy market-structure runtime block from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` (ZigZag tracker, major/minor swing plots, BOS/MSS line/label engine), retained minimal pivot context (`hv/lv`) for zone sub-conditions, and added `get_htfs()` selector for top-2 higher TFs from (`15`,`240`,`1D`,`1W`).
111. Replaced `get_trend_state()` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with MTF structure-bias aggregation using `request.security` pivot-break states on (`15`,`240`,`1D`,`1W`) filtered by `get_htfs()`.
112. Refactored shared drawing primitives in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `draw_line_segment`, `draw_text_on_line`, and expanded `draw_line_with_text` for horizontal/diagonal and `xloc`-agnostic usage.
113. Migrated liquidity base lines/sweep labels, EQ lines/labels, PDH/PDL/PWH/PWL lines/labels, and zigzag segments to shared draw helpers in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`.
114. Added shared creation helpers in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: `create_zone_visuals`, `add_primary_zone`, `draw_event_label`, `draw_swing_marker`, `create_htf_candle_objects`.
115. Routed OB/RJB/FVG zone construction to `add_primary_zone(...)` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to reduce repeated box/label object wiring while preserving detection logic.
116. Stabilized helperized zone visuals by restoring legacy label X offsets (OB `+3`, RJB `+11`, FVG `+7`) inside `add_primary_zone(...)` call sites.
117. Applied small token-trim cleanup in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed redundant helper parameters from `create_zone_visuals(...)`, `draw_event_label(...)`, and `draw_swing_marker(...)`, then updated all callers.
118. Moved reusable helper methods from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` to `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` (line/text draw helpers, event/swing marker helpers, HTF mapper, HTF span helper) and switched ICT call sites to `LIB.*`.
119. Added strict confirmation pipelines in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: sweeps now queue and confirm only on reclaim-close within a short window; breaks now queue explicit break-retest events and confirm only after retest + directional close, while keeping potential retest flow unchanged.
120. Normalized event helper naming to snake_case and moved queue registration/match helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` (`has_sweep_event_for`, `register_sweep_reclaim_event`, `has_break_retest_event_for`, `register_break_retest_event`), with ICT updated to shared `LIB.*` usage and `LIB` event types.
121. Added confirmed-bar gating for zone creation in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: OB/FVG/RJB condition helpers now require `barstate.isconfirmed`, preventing new zones from being created on open/incomplete candles (including HTF security context).
122. Added `ZONE_FORMATION_LOCK_BARS` in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with shared `zone_formation_allowed()` gating so OB/FVG/RJB creation ignores the most recent N candles (default `1`) to reduce fresh-zone noise.
123. Merged direct zone-respect confirms into unified pending-zone pipeline in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` by routing respect events through `register_zone_retest_signal(...)` + `checkConfirmations(...)`, and reordered runtime sequence to `_controlZone -> checkConfirmations -> sweep reclaim -> break-retest` for same-bar lifecycle consistency.
124. Updated trigger and key-level documentation: added explicit runtime execution sequence and implementation plan for remaining zone families (session/month/quarter key levels, structural S/R clusters, and supply/demand lifecycle integration).
125. Added canonical OB/SD overlap merge foundation in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: overlapping same-side OB/SD candidates now merge into one zone object (`OB` primary, `+SD` label tag), sharing one lifecycle and preventing duplicate boxes/signals.
126. Implemented live SD detection in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `plotSD` toggle and `Rally-Base-Drop / Drop-Base-Rally` detection with ATR base-compression + ATR impulse filters (+ relative-volume filter), wired to `add_primary_zone(..., \"SD\", ...)` and existing lifecycle/signal pipeline.
127. Tuned SD visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: switched pre-leg checks to wick-aware conditions, reduced impulse threshold, disabled SD volume filter by default, and relaxed SD creation gate to avoid over-filtering.
128. Implemented structural S/R in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added pivot-cluster S/R engine (`showSR`) with ATR-based price clustering, touch-strength labels (`R#`/`S#`), broken-level restyling, and capped/pruned S/R level lifecycle.
129. Refined S/R presentation/logic in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: switched SR lines to solid key-level color, changed labels to dynamic role-only (`R`/`S`) with bearish/bullish colors and `Small` size, and enabled runtime R<->S flipping by relative price position (`above=R`, `below=S`).
130. Added nearby-SR dedupe in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: SR levels within `ATR * smc_min_height` are merged to reduce clutter; retained level is the one closer to current price and keeps stronger touch score.
131. Applied zone-density optimization batch in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: introduced global lookback cap (`GLOBAL_MAX_LOOKBACK_BARS`), unified pivot-length base (`eqlPivotLen` dynamic), current-zone per-type/side cap (`3`) with quality+age importance pruning, HTF projection cap per side (`1`) with recent-time filter, and increased HTF zone background transparency.
132. Switched to fixed extension tiers in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: replaced dynamic strength-based extension at zone call sites with constants (current TF: FVG/iFVG=`1`, OB-family=`5`, SR+PDH/PDL=`20`; HTF projected: slot1=`10`, slot2=`15`).
133. Applied global lookback gating to structure visuals in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: BOS/MSS labels/lines, current+HTF swing markers, and zigzag segments now draw only inside `GLOBAL_MAX_LOOKBACK_BARS` via shared time/index cutoffs.
134. Unified per-side current-zone cap buckets in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: `FVG+iFVG` now share `FVG_FAMILY` cap and `OB+BB+RJB+SD` share `OB_FAMILY` cap to reduce overlap clutter and improve consistency.
135. Restored SR visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: lowered `SR_MIN_TOUCHES` from `2` to `1` so clustered levels render on first valid pivot touch while still showing touch-strength indexing (`R1/S1`, `R2/S2`, ...).
136. Improved SR touch logic and cap validity rules in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added SR touch-band detection (`price ± band`, ATR/tick constants), introduced `SR_TOUCH_MIN_COUNT`, and updated current-zone caps to count only valid/unmitigated zones (`not broken` and `not mitigated`).
137. Added HTF family-per-side selection and SR touch cooldown in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: HTF projected zones now pick nearest valid/unmitigated candidates per side and family (`OB_FAMILY`, `FVG_FAMILY`), and SR touch-strength increments are rate-limited by `SR_TOUCH_COUNT_COOLDOWN_BARS`.
138. Added configurable HTF render-slot policy in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: default projection now renders 2 slots per HTF (nearest above + nearest below) via `HTF_ZONE_RENDER_SLOTS_PER_TF`, with internal family-priority selection controls (`HTF_ZONE_FAMILY_PRIORITY_OB/FVG`) and optional expansion up to 4 internal slots.
139. Added SR visual-style constants and mitigated-zone blur tuning in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: SR now uses neutral blurred color + style-by-touch (`solid` before extra touches, `dotted` after), and mitigated zones now use `MITIGATED_ZONE_BG_TRANSP` (aligned with HTF blur level by default).
140. Hardened HTF render-slot fallback and mitigated border styling in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: clamped effective HTF render slots (`>=1`), improved candidate ordering fallback when one side is missing, and aligned mitigated zone border to SR light neutral style.
141. Updated SR semantics and BOS color alignment in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: first-touch SR labels now render as `IDM`, SR touch counting uses wick-based interaction with body-cross exclusion, and BOS/bos line color is aligned with SR neutral style.
142. Refined SR lifecycle rules in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: untouched SR levels now label as `idm` for current-TF source (or `IDM` for HTF source), untouched body-crossed levels are removed immediately, and touched-level behavior remains unchanged (`R/S` touch indexing).
143. Enforced global lookback pruning for EQ and liquidity/sweep in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: old EQ lines/labels and old liquidity/sweep visuals now delete when outside `GLOBAL_MAX_LOOKBACK_BARS`; EQ pivot caches are also gated to recent indices to reduce stale processing.
144. Decoupled HTF-zone lookback from global lookback in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `HTF_ZONE_LOOKBACK_BARS` (default `500`) and used it in HTF projection candidate filtering to prevent HTF zones from disappearing when `GLOBAL_MAX_LOOKBACK_BARS` is set very low.
145. Added dynamic HTF2-anchor lookback model in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: derived HTF2 candle span from latest HTF2 target anchor (zone/swing fallback), derived HTF1 right-side candle count from the same anchor span, and mapped HTF2 span into runtime current-TF lookback bars (`effectiveCurrLookbackBars`) for draw/performance gating.
146. Applied HTF candle visibility guardrails in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: tightened dynamic candle-count caps (`HTF1 max=16`, `HTF2 max=12`) to keep right-side HTF candle groups inside visible chart area with current render orientation.
147. Fixed HTF candle collapse-to-one issue in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: introduced stable HTF slot counts that refresh only on new HTF candle (`ta.change(o1/o2)`), preventing per-bar buffer resets that caused only the latest candle to remain fully rendered.
149. Fixed HTF2 one-candle visibility while preserving order in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: kept HTF2 positioned to the right of HTF1, but changed per-group candle layout to extend left from each group anchor so full candle history remains visible.
150. Hotfix for HTF candle overlap in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: recalculated HTF group anchor offsets from each group span so HTF1 starts to the right of real candles, and HTF2 stays to the right of HTF1 with configured gap.
151. Tuned HTF panel spacing and added temporary count debug in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: reduced `HTF_GROUP_GAP` (`10 -> 4`) and added top-right debug label (`HTF_DEBUG_SHOW_COUNTS`) showing live rendered candle counts for HTF1/HTF2.
152. Updated HTF spacing/count rule in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: set `HTF_GROUP_GAP=2` and changed HTF2 candle count to derive from HTF1 candle count by timeframe ratio (e.g., `4h -> 1D` approximately `/6`), clamped by HTF2 min/max constants.
153. Cleaned HTF panel debug and finalized spacing in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: removed temporary HTF count debug label logic and set `HTF_GROUP_GAP=5`.
154. Implemented dynamic lookback control framework in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added recalc cadence modes (`ON_HTF2_CLOSE` / `EVERY_N_BARS`), anchor-priority modes (`OB_FIRST` / `SR_FIRST` / `SWING_FIRST`), integrated HTF SR snapshot anchors, and moved HTF/current lookback + candle counts to cached dynamic state updates.
155. Performed shared-method and dead-code cleanup in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: extracted repeated line+label deletion into `delete_line_label(...)` and applied it across liquidity/EQ/SR cleanup paths; removed unused constants and dead helper methods (`get_zone_*extend*`, `get_sr_extend_bars`, stale extension defaults).
156. Moved pure helpers from ICT to shared library (`/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine`): exported and adopted shared helpers for timestamp fallback (`get_latest_time4`, `get_first_non_na3`), anchor candle sizing (`get_candles_to_anchor`), lookback conversion (`get_curr_lookback_from_htf2`), and visual cleanup (`delete_line_label`), with ICT callsites switched to `LIB.*`.
157. Migrated projected HTF box lifecycle to shared library in `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` via `update_projected_box(...)` + `update_projected_box_slot(...)`, and switched `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` projected-zone rendering callsites to `LIB.*`; HTF swing markers remain on label path (kept intentionally for accurate historical x-index placement where `plotchar` is not reliable with dynamic mapped indices).
158. Fixed typed-`na` compile issue after helper migration in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: updated `LIB.update_projected_box_slot(...)` fallback call to pass explicit casts (`int(na)`, `float(na)`), and documented the error pattern/prevention in `/Users/macmini/Trade/Bot/Hung Bot/ai-agent/errors/COMMON_ERRORS.md`.
159. Replaced fixed strict-confirm windows in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine` with timeframe-aware dynamic methods: added `get_dynamic_sweep_reclaim_window_bars()` and `get_dynamic_break_retest_window_bars()`, introduced min/max clamps (`SWEEP_RECLAIM_MIN/MAX_BARS`, `BREAK_RETEST_MIN/MAX_BARS`), and wired runtime confirmation processors to use per-bar dynamic windows.
160. Fixed repeated EQ runtime historical-buffer errors in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: added `EQ_SAFE_HISTORY_OFFSET` guards for `currOff/prevOff/checkOff`, pruned stale EQ pivot caches by offset age in `execute_eq_logic()`, and documented the pattern in `/Users/macmini/Trade/Bot/Hung Bot/ai-agent/errors/COMMON_ERRORS.md`.
161. Fixed bar-0 EQ pruning crash in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: rewrote `while array.size(...) > 0 and array.get(...size-1)` conditions into safe loop-body `array.get` checks to prevent `Index -1` out-of-bounds on empty arrays; added the pitfall to `/Users/macmini/Trade/Bot/Hung Bot/ai-agent/errors/COMMON_ERRORS.md`.
162. Started 3-file indicator split (seed phase) with independent entry scripts: created `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine` (structure-focused seed), `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine` (zones-focused seed), and `/Users/macmini/Trade/Bot/Hung Bot/src/ict_signals_dashboard.pine` (bias/dashboard-focused seed); updated headers and set zones file defaults to minimize overlap (`MS_SHOW_SWINGS/BOS_MSS=false`, `showBiasTable=false`, `showEQL=false`, `showSR=false`, `liquidity=false`, signal toggles off).
163. Reworked split to preserve original UI/style/constants by cloning from the same baseline core into all three split files, then applying ownership toggles only: `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine` (structure-only defaults), `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine` (zones/levels defaults, structure/dashboard off), `/Users/macmini/Trade/Bot/Hung Bot/src/ict_signals_dashboard.pine` (dashboard-only defaults, structure/zones/levels off).
164. Corrected split alignment after user feedback: restored split files from the same original core baseline again and re-applied ownership toggles with original style/constants preserved (`ict_structure_levels`: structure-only; `ict_zones_lifecycle`: zones/levels-only + HTF zones; `ict_signals_dashboard`: dashboard-only defaults).
165. Physical split pass-1 applied to `/Users/macmini/Trade/Bot/Hung Bot/src/ict_signals_dashboard.pine`: removed full zone-lifecycle runtime section (OB/FVG/RJB/SD detection, zone control, pending confirmations, signal candle coloring) and kept dashboard rendering path, moving file from toggle-only split toward true code separation.
166. Hard-pruned `/Users/macmini/Trade/Bot/Hung Bot/src/ict_signals_dashboard.pine` into a true standalone dashboard file (~200 lines): rebuilt script to keep only bias-confluence scoring + dashboard table with HTF context using `LIB.get_htfs()`/`request.security`, removing all zone/structure/event-lifecycle dependencies.
167. Physical split pass-2 applied to `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine`: removed zone/lifecycle/signal runtime tail and disabled liquidity/EQ/SR execution calls, keeping structure-centric runtime ownership (swings/BOS/MSS/zigzag context) while dashboard and zones execution are delegated to their dedicated files.
168. Physical split pass-3 applied to `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine`: removed structure drawing runtime (swing/BOS/MSS/zigzag and HTF mini-candle render path), removed dashboard function body/call, kept zone/level lifecycle runtime (OB/FVG/RJB/SD + EQ/SR/liquidity + HTF projected zones), and trimmed now-unused structure-state vars.
169. Real prune pass applied to split files: removed dead/unreachable functions and constants from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine` (including unused dynamic confirm-window helpers, OB/SD merge-only dead chain, and orphan signal-marker constants), removed orphan constants from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine`, and dropped unused structure-only inputs in structure file; result reduced split total from `5196` to `5035` lines with `0` def-only and `0` const-only leftovers by static scan.
170. True ownership prune phase-1 executed with reachability analysis: removed `58` unreachable function blocks from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine` and `10` unreachable function blocks from `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine` (physical deletion, not toggle-off).
171. True ownership prune phase-2 executed: removed post-prune orphan constants (`54` lines in structure file, `4` lines in zones file), leaving both split files at `0` def-only and `0` const-only by static scan; split size reduced to `3694` lines total.
172. True ownership prune phase-3 executed on `/Users/macmini/Trade/Bot/Hung Bot/src/ict_structure_levels.pine`: removed duplicated bias/HTF-projected-zone runtime branches and related dead declarations (including unused type blocks and single-use top-level settings/state), then re-pruned reachability; structure file reduced to `797` lines / `45,843` bytes, split total reduced to `3,279` lines / `330,747` bytes.
173. True ownership prune phase-4 executed on `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine`: removed additional single-use non-runtime declarations (unused type/top-level state/settings leftovers), then re-validated function reachability; zones file reduced to `2,245` lines / `123,550` bytes and split total reduced to `3,243` lines / `328,606` bytes with `0` def-only and `0` const-only in all active split files.
174. Hotfix for missing zones visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_zones_lifecycle.pine`: raised `GLOBAL_MAX_LOOKBACK_BARS` default (`50 -> 500`) so recent OB/FVG/RJB/SD lifecycle objects are not over-pruned by default.
175. `hung_*` cleanup + shared-helper preparation: removed redundant signal/alert/event leftovers from `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine` (unused `EVT_*`, alert inputs, signal arrays/types/reset block), and added reusable bias-confluence exports to `/Users/macmini/Trade/Bot/Hung Bot/versions/05-03/lib [hung].pine` (`get_bias_max_score`, `get_effective_bias_min_pct`, `get_bias_factor_values`, `get_bias_factor_scores`, `get_trend_data`) for next library publish/migration step.
176. Continued `hung_*` ownership cleanup on `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine`: removed remaining non-structure runtime leftovers (dead HTF-zone helper block and key-level drawing tail), then re-pruned single-use declarations; file reduced to `512` lines / `30,330` bytes, and total `hung_*` bundle reduced to `2,958` lines / `165,091` bytes with `0` def-only and `0` const-only across all three files.
177. Extended shared library model in `/Users/macmini/Trade/Bot/Hung Bot/versions/05-03/lib [hung].pine`: added exported common SMC types (`PriceZone`, `entrySignal`, `ConfluenceResult`) plus backward-compatible alias (`ConflunceResult`) with safe boolean defaults (`false`, not `na`) for Pine type-field compliance.
178. Versioning sync pass for library workflow: confirmed active `hung_*` scripts already import `hunghoxuan/TradingKit/9`, and bumped local library header metadata in `/Users/macmini/Trade/Bot/Hung Bot/versions/05-03/lib [hung].pine` to `@version: 1.4.6` with updated trace notes for newly added bias exports and shared types.
179. Migrated local type usage in `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine` from local declarations to library exports (`LIB.PriceZone`, `LIB.entrySignal`, `LIB.ConfluenceResult`): removed local duplicate type blocks and updated all array/parameter/constructor references to `LIB.*` namespaced types.
180. Hotfix for missing zone visibility in `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine`: decoupled current-TF zone pruning from dynamic HTF lookback via `CURR_ZONE_LOOKBACK_BARS`, and relaxed default impulse quality filter (`smc_filter_use_impulse=false`, `smc_filter_min_body_atr=0.35`) so OB/FVG/RJB/SD zones render consistently.
181. Completed lib-version and bias-helper migration pass: synced imports to `TradingKit/11` across `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine`, `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine`, and `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_signals_dashboard.pine`; removed duplicated local bias helper implementations from `hung_ict_smc` and switched runtime to `LIB.get_trend_data(...)` and `LIB.get_bias_max_score(...)`.
182. Round-2 duplication reduction: migrated dashboard bias stack in `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_signals_dashboard.pine` to `LIB` exports, added shared HTF/pivot/lookback/ledger helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` (`@version: 12`), switched `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine` to those shared methods, and updated all `hung_*` imports to `TradingKit/12`.
183. Library compile hotfix in `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine`: enabled `dynamic_requests = true` in `library(...)` so exported request-based helpers (`get_ohlc_for_tf`, `get_pivots_for_tf`) are valid.
184. Pine export-constraint hotfix: removed request-based exports from `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` (`get_ohlc_for_tf`, `get_pivots_for_tf`) because exported `request.*` expressions cannot depend on function arguments; restored local request wrappers in `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine` while keeping other shared helper deduplication.
185. Added `LIB.Settings` in `/Users/macmini/Trade/Bot/Hung Bot/src/lib [hung].pine` and migrated shared duplicated constants to Settings-backed values in `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_smc.pine`, `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_structure.pine`, and `/Users/macmini/Trade/Bot/Hung Bot/src/hung_ict_signals_dashboard.pine` (bias weights, HTF/dynamic lookback defaults, swing offsets, shared transparencies).
186. Continued Settings migration in active `src` indicators before merge-back (`/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`, `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Market Structure Levels.pine`, `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Dashboard.pine`): kept shared constants sourced from `LIB.Settings`, removed local dashboard/SMC bias-weight alias block, and kept local HTF request wrappers where Pine library export constraints apply.
148. Fixed HTF candle panel layout direction in `/Users/macmini/Trade/Bot/Hung Bot/src/ict_smc [hung].pine`: HTF candles now extend left from right anchor (newest at right edge), HTF label x-centering adjusted for leftward span, and HTF2 group offset moved left of HTF1 to avoid off-screen clipping.

187. Low-risk dedupe cleanup in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: removed dead signal-plan formatting constants, merged mirrored OB/FVG detectors into side-parameter helpers, merged SD demand/supply detection, merged buy/sell tooltip builders, merged bullish/bearish liquidity constructors, and merged active-trading high/low selector logic into one directional helper.

188. Reduction round-2 in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: merged mirrored EQH/EQL detection into `find_equal_level(...)`, merged sweep/break dynamic confirmation-window helpers into one parameterized method, and collapsed SR right/label X helpers into one `get_sr_x(...)` helper.

189. Reduction round-3 in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: merged mirrored selector scoring into `get_selector_score(...)`, added shared `is_selector_candidate_valid(...)`, and rewired active-trading bound selection to reuse one selector validity path across PriceZones, KeyLevels, and PD/PW fallback levels.

190. Migrated pure helper layer and shared enum vocabulary to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` (`TradingKit/24`): moved side-text, selector validity/compare, active-range, and body-cross helpers into the library, added exported `Const` type for shared signal/OB/stat constants, and kept only Pine compile-time-sensitive aliases (`ENTRY_MODE_*`, `SIG_*_COUNT`) local in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`.

191. First-wave standalone helper migration to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` (`TradingKit/24`): moved OB/SD family helpers, zone-cap family, zone/signal label mappings, OB class/border helpers, HTF slot extension helper, signal type/stat index helpers, and SR match helper into the library; rewired `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` to `LIB.*` usage while keeping lifecycle and detection logic local.

192. Migrated generic confirmation-window and wick-touch helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` (`TradingKit/25`): ICT now uses `LIB.get_dynamic_confirmation_window_bars(...)` and `LIB.is_level_wick_touch(...)`, leaving only lifecycle-specific SR handling local.

193. Migrated reusable UI/object helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` (`TradingKit/26`): moved PriceZone draw/delete helpers (`delete_zone_drawings`, `create_zone_visuals`, `get_zone_min_right_idx`, `get_zone_label_x`) and Entry cleanup helper (`delete_entry_visuals`) into the library, then rewired ICT runtime to `LIB.*` usage.
194. Migrated standalone validation/label helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` while keeping published import pinned at `TradingKit/24`: moved `get_base_bounds`, `check_zone_respect`, `is_signal_bias_valid`, `is_zone_above_price`, `get_zone_priority_score`, `get_sr_x`, `is_sr_source_htf`, `is_sr_res_now`, `get_sr_label_text`, and `record_swing_if`, then rewired ICT call sites to `LIB.*`.

195. Migrated another helper batch to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` while keeping published import pinned at `TradingKit/24`: moved zone formation/OB-FVG gating helpers, current-zone extension mapping, signal-matrix mutation, pending-entry lookup, active-range minimum-distance and selector-score helpers, plus the HTF mini-chart rendering stack (`get_htf_range`, `create_htf_candle_objects`, `render_htf_view`, `update_htf_name_label`, `render_htf_slot`) and removed the corresponding ICT-local duplicates.
196. In `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`, refactored additional helper methods to explicit-argument signatures (`is_sd_volume_ok`, `detect_sd_zone`, `is_signal_zone_enabled`, `find_entry_idx`, `prune_entries`, `has_nearby_ob`) and rewired call sites; also normalized reusable method headers with `Helper:` prefixes for clearer helper/event classification.
197. Migrated additional zone/entry helper implementations from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` into `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKit.pine` and rewired ICT to `LIB.*`: SD detection (`detect_sd_zone`, `is_sd_volume_ok`), unified mitigation (`check_mitigation`), zone lifecycle caps (`prune_*`, `add_active_zone`), OB proximity/classification (`has_nearby_ob`, `classify_new_ob`, `get_dynamic_anchor_ts`), and entry helpers (`is_signal_zone_enabled`, `find_entry_idx`, `prune_entries`); removed ICT-local duplicates.
198. Fixed a TradingKit dependency ordering issue: moved `zones_overlap()` above its first usage (`has_nearby_ob`) to avoid Pine library forward-reference errors; bumped TradingKit to `/28` and updated `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` import accordingly.
199. Fixed another TradingKit dependency ordering issue: moved `get_first_non_na3()` above its first usage (`get_dynamic_anchor_ts`) to avoid Pine library forward-reference errors; bumped TradingKit to `/29` and updated `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` import accordingly.
200. Reduced `request.security()` usage in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: removed fixed-timeframe prefetch blocks for HTF bias/zones/SR (`"15"`, `"240"`, `"1D"`, `"1W"`) and now request only the active `htf1`/`htf2` snapshots needed for the dashboard and projected zones (plus PDH/PDL/PWH/PWL requests).
201. Further reduced `request.security()` usage in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: replaced `get_htf_ohlc()` + `get_htf_pivots()` (2 calls per HTF) with a single `get_htf_bundle()` request per HTF that returns OHLC + pivot values + pivot timestamps/indices + HTF bar_index series.

## Full Remaining Queue (Current)
1. Compile-check `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` in TradingView after the merge-back and fix any compatibility/runtime issues.
2. Record TradingView compile token count for the merged ICT file and reassess token headroom.
3. Consolidate the mixed-source target selector into reusable `get_best_target_above(...)` / `get_best_target_below(...)` helpers so `activeTradingHigh/Low`, TP targeting, and future reaction logic share one ranking engine.
4. Formalize consume/rotation rules for `activeTradingHigh/Low` (`close through` first) so the next anchor can advance without adding duplicate ranking logic.

202. Updated `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` fib logic: removed previous/bigger-leg fib rendering (keep latest leg only), and added post-leg crossed-level suppression so 0.5/0.618/0.705 lines are hidden after price has crossed them from leg-end to current bar.

203. Split library namespace into `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine` (phase-1 baseline clone split), then repointed imports: ICT -> `hunghoxuan/TradingKitSMC/1`, Indicators -> `hunghoxuan/TradingKitCore/1`.
204. Added pre-split backups for all touched files: TradingKit, ICT, Indicators (`..._before_split_core_smc`).

205. Active-area selector updated in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: added major swing candidates (current/HTF1/HTF2) with bounded recent-count scoring, and changed SR first-touch body-cross handling from immediate deletion to weak/broken marking (dashed + dim) to keep SR lifecycle visible.

206. Added local unified selector model in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: new `KeyLevel` type + `collect_keylevels(...)` + `pick_best_keylevel(...)`, while keeping existing source arrays (`activeZones`, `srLevels`, `eqLevels`, `liquidityLevels`) unchanged; `get_active_trading_bounds()` now selects from this merged candidate view.

207. Updated ATH/ATL selector priorities and min-distance logic in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: HTF projected > HTF2 swing > HTF1 swing > OB > (FVG/RJB/current swing) > (Liquidity/EQ/SR), plus swing-guard min distance based on nearest current swing above/below; also increased current TF swing marker visibility.

208. Added `ZoneTypeConfig` pass-1 in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: introduced zone config registry (OB/FVG/RJB/SD/BB/IFVG), wired zone-id and strategy-id lookups through config, migrated zone extend reads and active-area zone scoring to config-driven values while keeping Strategy type unchanged.

209. Replaced `ZoneTypeConfig` dynamic array registry with fixed per-type config objects in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` (`cfgOB/cfgFVG/cfgRJB/cfgSD/cfgBB/cfgIFVG`) and switched `get_zone_cfg(...)` to O(1) branch dispatch to avoid config-loop overhead.

210. Removed redundant zone constants from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` (`ZONE_ID_*`, `ACTIVE_AREA_SCORE_PZ_*`), replaced with config/type-driven checks and `ACTIVE_AREA_SCORE_ZONE`, removed legacy `get_zone_id(...)`, and confirmed only one ATH/ATL selector pipeline remains (`collect_keylevels` -> `pick_best_keylevel` -> `get_active_trading_bounds`).

211. Refactored zone signal strategy metadata in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: removed `STRAT_ID_*` and zone `SIG_SCORE_*` constants, added `ZoneTypeConfig.sigScore`, replaced strat-id switches with `get_zone_strategy_meta(type)` and `get_sweep_strategy_meta()`, and removed legacy `get_strategy_id_from_zone/get_strategy_meta` path.

212. Entry planning updated in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: added SL anti-hunt gap (`ENTRY_SL_GAP_TICKS`), RR floor at 1.0, and optional dynamic TP (`ENTRY_DYNAMIC_TP_ENABLE`) that clamps TP to min(nearest ATH/ATL target, fixed RR TP) while enforcing minimum RR=1.

213. Fixed Pine compile order issue in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: replaced `add_entry` call to late-defined `get_active_trading_bounds()` with pre-declared `get_entry_tp_targets()` helper (nearest key-level scan) for dynamic TP clamping.

214. Updated shared entry tooltip in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine` (`draw_entry_visual`): now shows real RR computed from actual entry/TP/SL and shortened text format: `BUY/SELL (Setup)` + one-line prices + reason.

215. Entry tooltip format adjusted in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine` to: `BUY/SELL (Setup: xxx)`, `Entry ... TP/SL ... (RR real)`, and multi-line reason list by normalizing `+`/`,` separators into bullet lines.
216. Entry tooltip format refined in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine` to compact style: `B/S (setup)`, `e/tp/sl/rr` on one line, then `R:` first reason + `-` bullet lines for remaining reasons; confirmed `ENTRY_SL_GAP_TICKS` default in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` is `3.0`.
217. Unified candle-pattern config source into LIB: moved CP thresholds/volume gates to shared `Settings` and added `get_candle_pattern_cfg(...)` helper in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`; removed local CP cfg-init/constants from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine`.
218. Removed CP-settings duplication from `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`: `TradingKitSMC.get_candle_pattern_cfg(...)` now sources defaults from `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` (`CORE.Settings`) and ICT callsite updated to new signature.
219. One-pass CP helper migration: moved remaining candle-pattern visual/conviction helpers to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` (`CandlePatternVisualCfg`, `get_candle_pattern_visual_cfg`, `cp_get_volume_context`, `cp_should_show`, `cp_normal_body_color`, `cp_volume_label`) and removed local CP visual constants from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine`; updated imports to `TradingKitCore/2`.
220. Moved TPG candle-action checks to `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` (`tpg_rsi_state`, `tpg_engulfing`, `tpg_hammer_shooting`, `tpg_two_bar_reversal`) and refactored `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine` to call these helpers; bumped Core import usage to `TradingKitCore/3` in Indicators and TradingKitSMC.
221. ATH/ATL selector state filters added in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: mitigated OB is ignored by default (`ACTIVE_AREA_SKIP_MITIGATED_OB=true`), other mitigated zones/key-levels are down-weighted, and weak-quality candidates are penalized via centralized selector-score helpers.
222. Pivot policy updated in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: current-TF pivot is fixed (`PIVOT_LEN_BASE=15`) for stable context, while HTF mimic pivots now use dynamic length by target TF (`get_dynamic_pivot_length(targetTf)`), removing ratio-based scaling from HTF mimic detection.
223. Refined relative-pivot model: `get_dynamic_pivot_length` now takes `(currentTf, targetTf)` in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`, returning higher pivots for farther HTF distance; ICT updated to use this for HTF mimic, and current fixed pivot default adjusted to `PIVOT_LEN_BASE=10`.
224. Reduced `TradingKitCore` local scope pressure by removing `_pivot_len_bucket(...)` helper and inlining its bucket logic in `get_dynamic_pivot_length(...)` to address scope-limit overflow (551 -> target under 550).
225. Added HTF-to-active merge mode in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: when `HTF_MERGE_INTO_ACTIVE_ZONES=true`, HTF OB/FVG snapshots are injected into `activeZones` after prune using TF-prefixed labels (`4h OB`, `1D FVG`) and processed by normal zone lifecycle; projected HTF box drawing is disabled in this mode to avoid duplicate visuals.
226. Unified zone-label path in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: rendering now uses `labelText` only (no OB-specific draw-time rewrite), OB class labels normalized to prefix style (`d.OB/o.OB/x.OB/m.OB`), HTF merged labels switched to `TF.TYPE` (e.g. `15m.OB`), and right-edge padding fixed by anchoring label X inside box-right.
227. Corrective note: pivot behavior now supersedes item 222 — current TF uses fixed context pivot (`PIVOT_LEN_BASE=10`), and HTF mimic pivots use relative function `get_dynamic_pivot_length(currentTf, targetTf)`; additionally hardened OB label utilities for Pine string safety (no `str.starts_with/ends_with`, guarded `str.substring` bounds).
228. Added per-timeframe market-structure visual toggles in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: independent on/off for swings and zigzag on current TF / HTF1 / HTF2; added HTF1 zigzag (light purple, solid, width 1) and HTF2 zigzag (light blue, dashed, width 2) drawn from mimic HTF swing pivots.
229. Increased structure/zigzag visibility window in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` by adding `STRUCTURE_LOOKBACK_MULT=2.0` and applying it to structure recent cutoffs (`structRecentTsCut`, `structRecentIdxCut`), so BOS/MSS, swings, and zigzag can render more history while keeping safety limits.
230. Refactor pass-1 for TF-unified structure visuals in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: introduced `Swing`, `TFConfig`, and `TFData` types; moved per-TF zigzag/swing runtime state into `ltfData/htf1Data/htf2Data` and switched current/HTF swing + zigzag drawing block to consume these objects via shared config (`ltfCfg/htf1Cfg/htf2Cfg`).
231. Refactor pass-2 for TF-unified structure state in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: moved `highArmed/lowArmed/prevBreakout/msBias` into `TFData` (`ltfData/htf1Data/htf2Data`), rewired BOS/MSS process calls to use object fields, and removed legacy duplicated state globals (`curr*`, `htf1*`, `htf2*` structure-state vars).
232. Refactor pass-3 for TF-unified swing ledgers in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: moved swing arrays (`swingHighPrices/swingHighTimes/swingLowPrices/swingLowTimes`) into `TFData`, rewired structure processing, fib leg extraction, and active-trading keylevel collection to `ltfData/htf1Data/htf2Data` ledgers, and removed legacy duplicated per-TF swing-array globals.
233. Refactor pass-4 for TF-unified structure flow in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: added reusable `update_tf_armed_state(...)` and `update_tf_structure_state(...)` helpers, then replaced repeated LTF/HTF1/HTF2 arming + BOS/MSS state-update blocks with these helpers while preserving behavior.
234. Refactor pass-4b for TF-unified visual flow in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: added `update_tf_swing_markers(...)` and `update_tf_zigzag_from_pivots(...)` helpers and replaced duplicated HTF1/HTF2 swing-marker + zigzag update blocks with shared helper calls.

235. Refactor pass-5 for TF-unified zigzag flow in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: current-TF zigzag update now reuses `update_tf_zigzag_from_pivots(...)` (same path as HTF1/HTF2), removing bespoke LTF zigzag mutation block while preserving visual behavior.

236. Refactor pass-6 in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: added TFData state-apply helpers (`apply_tf_armed_state`, `apply_tf_structure_state`, `apply_tf_seen_state`, `apply_tf_zigzag_state`) and replaced repeated per-TF field assignment blocks with helper calls to reduce duplication while preserving flow.

237. One-pass refactor queue completed (no feature change): in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` unified TF pipeline writes into mutating helpers (`process_tf_armed_state`, `process_tf_structure_state`, `process_tf_swing_markers`, `process_tf_zigzag_from_pivots`) and removed tuple/apply assignment boilerplate; added Core/Helper role comments on key methods. In `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine` trimmed one redundant helper (`get_safe_rr`) by inlining, and normalized method role comments.

## Full Remaining Queue (Current)
1. TradingView compile/runtime validation for `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` and `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine` after pass-237.
2. If stable, move next safe standalone helpers from indicators into TradingKitCore only where it reduces duplicate logic without adding request/security complexity.
3. Optional final token/scope trim pass: remove single-use wrapper methods that only forward to `LIB.*`.

238. Final trim + small helper migration: in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine` removed trivial local wrappers (`strategy_bias_allows`, `is_strategy_applicable`, `make_entry_signal`) and rewired to library calls; added SMC import (`TradingKitSMC/7`). In `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine` bumped header to `@version: 7` and added exported helpers `strategy_is_applicable(...)` and `make_event_signal(...)` for shared strategy/event flow.

239. Scope-limit hotfix: reverted the last TradingKitSMC helper migration to fix `Script has too many local scopes` (552>550). Removed `strategy_is_applicable` and `make_event_signal` from `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`, restored lightweight local wrappers in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine`, and removed SMC import there.

240. Added standalone swing-ledger sanitizer in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: `sanitize_swing_ledgers(...)` enforces alternating sequence by collapsing consecutive HH/LL duplicates (keep higher-high / lower-low), then rewired `process_tf_armed_state(...)` to sanitize after each swing-record update for LTF/HTF1/HTF2 ledgers.

241. Zigzag/swing alignment fix: `process_tf_swing_markers(...)` and `process_tf_zigzag_from_pivots(...)` in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` now process only pivots accepted by `sanitize_swing_ledgers(...)` (time must match latest sanitized ledger point), so collapsed HH/LL duplicates no longer leak into HTF/LTF swing markers or zigzag segments.

242. Added unified live/fake/latest swing path across TFs in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: introduced `process_tf_live_tail(...)` and applied it to LTF/HTF1/HTF2 (live zigzag tail + optional live swing marker). Also tied swing markers and zigzag updates to sanitizer-accepted pivots only.

243. Safe dead-constant cleanup pass completed. Removed unused constants from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine` (`PIVOT_MODE_DYNAMIC`, `SR_TOUCH_MIN_COUNT`, `SIGNAL_PLAN_EXTEND_BARS`, `ENTRY_RESOLVED_DISPLAY_FULL`, `ENTRY_RESOLVED_DISPLAY_MARK`, `ENTRY_RESOLVED_DISPLAY_MODE`, `ENTRY_RESOLVED_LINE_BARS`) and from `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine` (`PIVOT_PERIOD_LONG`, `SIGNAL_PLAN_EXTEND_BARS`, `ENTRY_RESOLVED_DISPLAY_FULL`, `ENTRY_RESOLVED_DISPLAY_MARK`, `ENTRY_RESOLVED_DISPLAY_MODE`, `ENTRY_RESOLVED_LINE_BARS`, `EVENTS_TEXT`). No logic changes.

244. Unified active-area selector scoring in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: replaced parallel methods (`get_zone_selector_score`, `get_keylevel_selector_score`) with one `get_selector_score_unified(...)`, and rewired `collect_keylevels(...)` for zone/key-level candidates through the same scoring path.

245. SR pipeline refactor in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: replaced pivot-based SR seeding with TFData swing seeding (`HTF2 -> HTF1 -> LTF`), added virtual SR band (`top/bottom`) for overlap filtering, added per-level reject counters via `srRejectCounts`, and unified SR tooltip to show both Touches and Rejects. Existing SR line rendering kept.

246. SR lifecycle normalization in `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`: introduced shared lifecycle states (`FORMED`, `TOUCHED`, `RESPECTED`, `BROKEN`, `RECLAIMED`) and applied them to SR with aligned state storage (`srStates`) and reject counters; SR visuals are now mapped from lifecycle state while keeping line-based rendering. Added `SR_USE_LTF_SEEDS` (default `false`) so SR seeding can run HTF-only (`HTF2 > HTF1`) with optional LTF inclusion.

247. Added backup set before visual-config refactor:
- `/Users/macmini/Trade/Bot/Hung Bot/backups/TradingKitCore.pine.bak_20260316_103024_visual_cfg_pass`
- `/Users/macmini/Trade/Bot/Hung Bot/backups/TradingKitSMC.pine.bak_20260316_103024_visual_cfg_pass`
- `/Users/macmini/Trade/Bot/Hung Bot/backups/Hung - ICT SMC Zones.pine.bak_20260316_103024_visual_cfg_pass`
- `/Users/macmini/Trade/Bot/Hung Bot/backups/Hung - Indicators.pine.bak_20260316_103024_visual_cfg_pass`

248. Visual-config unification pass (one-shot) in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine` and ICT wiring:
- Added configurable types for reusable visuals: `ZoneVisualCfg`, `ProjectedBoxVisualCfg`, `LineTextVisualCfg`, `PriceZoneLineVisualCfg`.
- Added cfg-first methods + compatibility wrappers:
  - `create_zone_visuals_cfg(...)` (+ existing `create_zone_visuals(...)` wrapper)
  - `update_projected_box_cfg(...)`, `update_projected_box_slot_cfg(...)` (+ existing wrapper methods)
  - `draw_line_with_text_cfg(...)` (+ existing `draw_line_with_text(...)` wrapper)
  - `apply_line_pricezone_visual_cfg(...)` (+ existing `apply_line_pricezone_visual(...)` wrapper)
- ICT now uses cfg methods for major zone/line rendering paths (current zones, projected HTF boxes, SR line visuals, EQ/liquidity/event lines).
- Existing call paths remain backward-compatible through wrappers.

249. Library/import sync after Core visual-config update:
- Bumped Core header to `@version: 26` in `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine`.
- Updated imports to Core/26 in:
  - `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitSMC.pine`
  - `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - ICT SMC Zones.pine`
  - `/Users/macmini/Trade/Bot/Hung Bot/src/Hung - Indicators.pine`

250. API cleanup pass for visual configs (requested no `_default`/`_cfg` redundancy):
- In `/Users/macmini/Trade/Bot/Hung Bot/src/TradingKitCore.pine`, original method names now accept config objects directly:
  - `create_zone_visuals(..., ZoneVisualCfg cfg)`
  - `update_projected_box(..., ProjectedBoxVisualCfg cfg)`
  - `update_projected_box_slot(..., ProjectedBoxVisualCfg cfg)`
  - `draw_line_with_text(..., LineTextVisualCfg cfg, Theme theme)`
  - `apply_line_pricezone_visual(..., PriceZoneLineVisualCfg cfg)`
  - `draw_entry_visual(..., EntryVisualCfg vcfg, ...)`
- Removed redundant wrappers/aliases: `*_cfg` and `draw_entry_default*`.
- Rewired ICT/Indicators callsites to pass config objects.
- Bumped Core header to `@version: 27` and updated imports in ICT/Indicators/SMC to Core/27.
